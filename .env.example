# Gan Batuach environment template
# Copy to .env.local for local development or .env.production.local for Docker.
# Never commit real secret values.

# REQUIRED: explicit environment identity. Keep browser/server values equal.
# Allowed values: local, demo, staging, pilot, production.
APP_ENV=demo
NEXT_PUBLIC_APP_ENV=demo

# REQUIRED, PUBLIC: Supabase project URL used by browser and server.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# REQUIRED, PUBLIC: Supabase publishable/anon key used by browser and server.
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-key

# REQUIRED, SERVER-ONLY: Supabase service role key for admin provisioning, lead conversion, secure storage uploads and server-side data access.
SUPABASE_SERVICE_ROLE_KEY=replace-with-server-only-service-role-key

# REQUIRED, PUBLIC: canonical app URL for redirects and links.
NEXT_PUBLIC_APP_URL=https://app.your-domain.co.il

# REQUIRED, SERVER-ONLY: same canonical app URL for server-side jobs and health tooling.
APP_URL=https://app.your-domain.co.il

# REQUIRED, SERVER-ONLY: Supabase auth redirect URL configured in Supabase dashboard.
AUTH_REDIRECT_URL=https://app.your-domain.co.il/auth/callback

# OPTIONAL, SERVER-ONLY: future Digital Observer host routing. Leave empty until Vercel/DNS are configured.
# Comma-separated values are supported for preview/staging host readiness.
DIGITAL_OBSERVER_STANDALONE_ENABLED=false
DIGITAL_OBSERVER_CUSTOM_DOMAIN_ENABLED=false
DIGITAL_OBSERVER_SEPARATE_BILLING_ENABLED=false
DIGITAL_OBSERVER_SEPARATE_SUPABASE_ENABLED=false
DIGITAL_OBSERVER_SOFT_LAUNCH=false
DIGITAL_OBSERVER_COMMERCIAL_LAUNCH=false
DIGITAL_OBSERVER_PRODUCT_MODE=route_only
DIGITAL_OBSERVER_PUBLIC_HOST=
DIGITAL_OBSERVER_APP_HOST=
GAN_BATUACH_PUBLIC_HOST=
DIGITAL_OBSERVER_PAYMENT_PROVIDER=
DIGITAL_OBSERVER_PAYMENT_MODE=disabled
DIGITAL_OBSERVER_INVOICE_PROVIDER=
DIGITAL_OBSERVER_CAMERA_GATEWAY_URL=
DIGITAL_OBSERVER_AI_PROVIDER=
DIGITAL_OBSERVER_SUPPORT_EMAIL=
DIGITAL_OBSERVER_DEFAULT_PACKAGE=home_basic
DIGITAL_OBSERVER_GAN_BATUACH_INTEGRATION_ENABLED=false
DIGITAL_OBSERVER_INTEGRATION_TOKEN_HASH=

# OPTIONAL, SERVER-ONLY: Google Places (New) address autocomplete and geocoding.
# Restrict this key in Google Cloud to the Places API and this server's egress/use case.
GOOGLE_MAPS_PLATFORM_API_KEY=

# REQUIRED FOR MOBILE BUILDS, BUILD-TIME: URL loaded by Capacitor native shell.
CAPACITOR_SERVER_URL=https://app.your-domain.co.il

# REQUIRED, SERVER-ONLY: protects scheduled cron routes.
CRON_SECRET=replace-with-long-random-secret

# REQUIRED, SERVER-ONLY: protects deep infrastructure health checks.
HEALTHCHECK_SECRET=replace-with-long-random-secret

# REQUIRED, SERVER-ONLY: legacy fallback field encryption for camera/device secrets. Do not rotate without a migration plan.
FIELD_ENCRYPTION_KEY=replace-with-32-byte-random-secret

# REQUIRED, SERVER-ONLY: authenticated field encryption for medical, identity and signature metadata.
# Never prefix these with NEXT_PUBLIC and never log them.
FIELD_ENCRYPTION_KEY_CURRENT=replace-with-long-random-current-field-encryption-secret
FIELD_ENCRYPTION_KEY_VERSION=v1
FIELD_ENCRYPTION_KEY_PREVIOUS=
FIELD_HASH_PEPPER=replace-with-long-random-server-only-lookup-hash-pepper

# OPTIONAL, PUBLIC: enable sandbox-only UI diagnostics. Must be false in production.
NEXT_PUBLIC_SANDBOX_MODE=false

# Digital Observer sign-up always uses Supabase email confirmation. Do not add
# a production bypass; configure the Confirm signup template and SMTP provider.

# OPTIONAL, SERVER-ONLY: real video gateway for RTSP/ONVIF/DVR/NVR to browser-safe playback.
CAMERA_GATEWAY_URL=
CAMERA_GATEWAY_PUBLIC_BASE_URL=
CAMERA_GATEWAY_SECRET=
CAMERA_TOKEN_SECRET=
CAMERA_STREAM_TOKEN_TTL_SECONDS=300
CAMERA_SNAPSHOT_STORAGE_RLS_VERIFIED=false
CAMERA_PROVIDER=custom
CAMERA_TEST_STREAM_URL=
DIGITAL_OBSERVER_CAMERA_MODE=disabled
VIDEO_GATEWAY_URL=https://video.your-domain.co.il
VIDEO_GATEWAY_PUBLIC_URL=https://video-public.your-domain.co.il

# OPTIONAL, SERVER-ONLY: video gateway provider and API key for MediaMTX/go2rtc/custom gateway.
VIDEO_GATEWAY_PROVIDER=custom
VIDEO_GATEWAY_API_KEY=
DVR_EXPECTED_CHANNEL_COUNT=16
VIDEO_GATEWAY_CLOUD_DISCOVERY_SECRET=
VIDEO_GATEWAY_CLOUD_ALLOWED_GATEWAYS=

# OPTIONAL, SERVER-ONLY: shared secret for video gateway callbacks and health checks.
VIDEO_GATEWAY_SIGNING_SECRET=replace-with-long-random-secret

# OPTIONAL, SERVER-ONLY: global provider safety modes. Defaults must stay safe until production activation is explicitly approved.
COMMUNICATIONS_SEND_MODE=mock
PAYMENT_MODE=disabled
INVOICE_MODE=mock
EMAIL_MODE=mock
SMS_MODE=mock
WHATSAPP_MODE=mock
PUSH_MODE=disabled
CAMERA_GATEWAY_MODE=disabled
AI_PROVIDER_MODE=mock

# REQUIRED ONLY FOR AN EXPLICIT PRODUCTION ACTIVATION. Leave these false/empty
# in local, demo, staging and pilot environments.
PRODUCTION_ACTIVATION_APPROVED=false
LIVE_ACTIVATION_CONFIRM=
LIVE_PAYMENTS_ENABLED=false
PRODUCTION_INVOICES_ENABLED=false
PARENT_CAMERA_VIEWING_ENABLED=false
LIVE_AI_ENABLED=false
PUSH_REAL_SEND_ENABLED=false

# OPTIONAL, SERVER-ONLY: communication provider mode. Default mock mode writes logs only and sends no real messages.
COMMUNICATION_PROVIDER=mock
SMS_PROVIDER=
SMS_API_KEY=
SMS_FROM_NUMBER=
SMS_PROVIDER_ACCOUNT_ID=
SMS_SENDER_ID=
SMS_REAL_SEND_ENABLED=false
VONAGE_API_KEY=
VONAGE_API_SECRET=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
SMS_WEBHOOK_SECRET=
WHATSAPP_PROVIDER=
WHATSAPP_TOKEN=
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_BUSINESS_ACCOUNT_ID=
WHATSAPP_APP_ID=
WHATSAPP_WEBHOOK_VERIFY_TOKEN=
WHATSAPP_REAL_SEND_ENABLED=false
META_WHATSAPP_TOKEN=
META_WHATSAPP_PHONE_NUMBER_ID=
META_WHATSAPP_BUSINESS_ACCOUNT_ID=
TWILIO_WHATSAPP_SID=
TWILIO_WHATSAPP_TOKEN=
TWILIO_WHATSAPP_FROM=
EMAIL_PROVIDER=
EMAIL_API_KEY=
EMAIL_FROM=
EMAIL_FROM_ADDRESS=
EMAIL_FROM_NAME=Gan Batuach
EMAIL_REPLY_TO=
EMAIL_REAL_SEND_ENABLED=false
RESEND_API_KEY=
SENDGRID_API_KEY=
SENDGRID_WEBHOOK_SECRET=
RESEND_WEBHOOK_SECRET=

# Sentry error/performance monitoring. The DSN is intentionally public; the auth token is server-only and used only during builds for source maps.
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
SENTRY_AUTH_TOKEN=
AWS_SES_REGION=
SES_REGION=
AWS_SES_ACCESS_KEY_ID=
AWS_SES_SECRET_ACCESS_KEY=

# OPTIONAL, SERVER-ONLY except VAPID_PUBLIC_KEY if exposed through a dedicated client config endpoint later.
# Push defaults to mock mode and sends no real Web/Android/iOS push without provider setup.
PUSH_PROVIDER=mock
PUSH_REAL_SEND_ENABLED=false
FCM_SERVER_KEY=
FCM_PROJECT_ID=
FCM_CLIENT_EMAIL=
FCM_PRIVATE_KEY=
FCM_SERVICE_ACCOUNT_JSON=
APNS_KEY_ID=
APNS_TEAM_ID=
APNS_BUNDLE_ID=
APNS_PRIVATE_KEY=
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
PUSH_PROVIDER_ENDPOINT=
PUSH_PROVIDER_API_KEY=
PUSH_WEBHOOK_SECRET=

# OPTIONAL, SERVER-ONLY: payment providers. Never store or commit raw card data; use provider tokenization only.
PAYMENT_PROVIDER=
PAYMENT_API_KEY=
PAYMENT_PUBLIC_KEY=
PAYMENT_WEBHOOK_SECRET=
PAYMENT_SUCCESS_URL=
PAYMENT_CANCEL_URL=
TRANZILA_TERMINAL=
MESHULAM_API_KEY=
CARDCOM_TERMINAL=
PELECARD_TERMINAL=

# OPTIONAL, SERVER-ONLY: invoice provider readiness.
INVOICE_PROVIDER=
INVOICE_API_KEY=
INVOICE_WEBHOOK_SECRET=

# OPTIONAL, SERVER-ONLY: external provider callback readiness.
AI_PROVIDER_WEBHOOK_SECRET=
AI_WEBHOOK_SECRET=

# OPTIONAL, SERVER-ONLY: real AI/smart observer gateway. If missing, the app uses deterministic rule-based insights.
AI_GATEWAY_URL=https://ai.your-domain.co.il

# OPTIONAL, SERVER-ONLY: protects AI observer ingestion route.
AI_OBSERVER_SECRET=replace-with-long-random-secret

# OPTIONAL, SERVER-ONLY: local vision detector mode. Default is safe shadow mock. Future values: local_opencv, local_yolo, local_http.
AI_PROVIDER=
AI_INFERENCE_ENDPOINT=
AI_MODEL_VERSION=
AI_FRAME_SAMPLE_RATE_SECONDS=30
AI_CONFIDENCE_THRESHOLD=0.75
AI_SHADOW_MODE_ENABLED=true
DIGITAL_OBSERVER_AI_MODE=mock
GAN_BATUACH_AI_LEGAL_MODE=israel_restricted
AI_REVIEW_QUEUE_ENABLED=true
AI_SAFE_TEST_FRAME_PATH=
AI_TEST_FRAME_STORAGE_PATH=
LOCAL_VISION_PROVIDER=local_mock
VISION_PROVIDER=local_mock
LOCAL_VISION_ENABLED=false
LOCAL_VISION_ENDPOINT=
CUSTOM_VISION_ENDPOINT=
VISION_SHADOW_MODE=true
VISION_HUMAN_REVIEW_REQUIRED=true

# OPTIONAL, SERVER-ONLY: external AI provider keys. Leave empty to avoid external AI calls.
OPENAI_API_KEY=
AI_PROVIDER_API_KEY=

# OPTIONAL, SERVER-ONLY: passkey relying party overrides.
PASSKEY_RP_NAME=גן בטוח
PASSKEY_RP_ID=your-domain.co.il
PASSKEY_ORIGIN=https://app.your-domain.co.il

# OPTIONAL, SERVER-ONLY: demo seeding only. Never set production user passwords here.
DEMO_BATCH_ID=
SEED_ADMIN_PASSWORD=
SEED_INSPECTOR_PASSWORD=
SEED_MANAGER_PASSWORD=
SEED_PARENT_PASSWORD=
SEED_STAFF_PASSWORD=

# REQUIRED SUPABASE STORAGE BUCKETS
# Create as private buckets unless explicitly documented otherwise:
# - profile-photos
# - child-photos
# - pickup-person-photos
# - kindergarten-logos
# - documents
# - camera-snapshots
