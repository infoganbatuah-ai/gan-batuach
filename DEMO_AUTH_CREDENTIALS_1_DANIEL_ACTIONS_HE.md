# DEMO AUTH CREDENTIALS 1 - פעולות לדניאל

## מה לעשות עכשיו

1. קובץ `.env.qa-demo.local` כבר קיים.

2. סיסמאות הדמו מולאו מקומית לפי שמות משתנים. לא צריך לשלוח אותן בצ'אט.

3. אל תעלה את הקובץ לגיט. הוא מוחרג מגיט.

4. ודא שיש משתמשים חסרים/לא מאומתים בתוך Supabase:

- הורה לא משויך
- צוות לא משויך
- מפקחת לא משויכת
- Digital Observer

5. אם הם לא קיימים, צור אותם ידנית ב-Supabase או הרץ את הסקריפט:

```bash
npm run qa:create-demo-role-users
```

רק אחרי שהוספת מקומית בלבד את `NEXT_PUBLIC_SUPABASE_URL` ו-`SUPABASE_SERVICE_ROLE_KEY`.

6. בין בדיקות תפקידים, פתח:

```text
/api/auth/logout
```

זה יוציא את המשתמש הנוכחי ויחזיר למסך התחברות.

7. שלח ל-Codex את הסטטוס:

- האם המשתמשים נוצרו/אומתו ב-Supabase: כן/לא
- האם הורה לא משויך נוצר: כן/לא
- האם צוות לא משויך נוצר: כן/לא
- האם מפקחת לא משויכת נוצרה: כן/לא
- האם Digital Observer נוצר: כן/לא
- האם להריץ AUTHED UX/UI QA 3: כן/לא

## הערה על Digital Observer

Digital Observer אינו משתמש של Supabase Dashboard. זה משתמש של האפליקציה שמוגדר ב-Supabase Auth ונכנס דרך מסך הכניסה של גן בטוח.

אם אתה משתמש במייל האישי שלך כדי להתחבר ל-Supabase עצמו, זה לא מספיק בשביל QA של הדשבורד באפליקציה. בשביל QA נקי עדיף ליצור משתמש דמו נפרד:

`qa.digital.observer@demo.ganbatuach.com`

## חשוב

לא להשתמש במשתמשים אמיתיים.
לא להשתמש בילדים אמיתיים.
לא להפעיל תשלומים חיים.
לא להפעיל מצלמות להורים.
לא להפעיל AI חי.
לא להפעיל WhatsApp/SMS production.
