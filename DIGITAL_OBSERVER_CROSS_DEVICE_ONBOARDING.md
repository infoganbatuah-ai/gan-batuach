# Cross-device onboarding — PUSH 17B

## PUSH 17D STATUS

INTERNAL FLOW READY; real mobile/Windows execution remains external. The mobile/web and computer use one canonical install intent, actor and Site. Pairing is ten-minute, single-use and replay protected. The computer claims through the native application, the original Product actor approves, a fresh healthy heartbeat yields CONNECTOR_FOUND, and the same wizard receives automatic discovery results. Multi-camera selection/configuration is now supported; earlier limitations below are retained as superseded history.

## STATUS

FOUNDATION IMPLEMENTED; complete customer handoff NOT READY. No real mobile/Windows install test. Web LAN discovery is not falsely advertised as native mobile capability.

## MOBILE-STARTED ONBOARDING

Same Add Cameras page, authorized existing Site and canonical planner. Unknown system gets manufacturer/app guidance, not protocols. No new Home.

## ZERO-INSTALL MOBILE FLOW

When an implemented, authorized, secure persistent path exists, use account authorization/discovery/confirmation without a computer. Current server has no verified executable cloud adapter. Temporary LAN visibility is not persistent monitoring.

## CONNECTOR-REQUIRED FLOW

Ask about an always-available supported computer. Conditional handoff can generate a document for an already installed app; public installer delivery remains missing. Do not market this as zero-touch.

## SEND / OPEN ON COMPUTER

Use native browser file sharing where available; otherwise download `.observer-connect` for user-controlled transfer. No email/SMS provider enabled. Document contains only short-lived installation authority; keep out of analytics/support/logs. It does not contain permanent camera/device credentials.

## PAIRING / SECURITY

Actor/Site are server-owned. Ten-minute atomic single-use claim; original logged-in actor must approve pending computer for original Site. Wrong account cannot inspect/approve; membership is rechecked at claim. Unknown client fields are rejected. New table RLS denies direct anon/authenticated access; RPC is service-only with fixed search_path.

## SESSION CONTINUITY

Only intent UUID goes into page history, never bearer. Server-rendered Add Cameras restores that intent and Site. Polling every five seconds while visible requires fresh delivered heartbeat to show CONNECTOR_FOUND. This resumes installation stage, not yet every stage of a persisted universal session.

## CONNECTOR DETECTED / MOBILE RESUMES

Same wizard renders existing camera discovery after fresh heartbeat. Native runner is designed to heartbeat and initiate existing discovery before camera selection. Full native install→discovery→selection→ACTIVE has NOT been executed. Candidate publication is bounded to eight and runtime configuration supports one camera; multi-camera commercial support remains incomplete.

## NO-COMPUTER FLOW

Only a genuinely local requirement with no suitable host can justify appliance recommendation. Unknown vendor capability or missing adapter must not become technical hardware necessity. Phone is never a permanent bridge.

## VALIDATION

13 deterministic commercial tests plus six isolated PostgreSQL subtests passed. These are not real mobile install proof. Cross-device download, OS approvals, account mismatch UI, reboot and activation remain acceptance gaps.
