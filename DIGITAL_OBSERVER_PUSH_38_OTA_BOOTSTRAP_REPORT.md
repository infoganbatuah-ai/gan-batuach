# PUSH 38E/F — OTA bootstrap report

## PUSH 38F status (2026-09-13)

**NOT READY FOR LIVE BOOTSTRAP.** No Home runtime, LaunchAgent, Device identity or camera configuration was changed. The old live health contracts and absent live OTA pointers remain. No pre-soak or v8 was started.

The PUSH 19 release contract uses Ed25519 over `canonicalEdgeUpdateManifest()` (sorted JSON excluding `signature`), with a `signing_key_id` looked up in a caller-supplied public SPKI registry. Artifact bytes are SHA-256/size verified. Unknown/revoked keys are rejected by removal from that registry. There is no Production release-custodian account, protected CI/HSM/KMS signer, audited approval/publication workflow, or deployed trusted-key rotation path in this checkout. These require an authorized CI/release operator to provision a protected Production Ed25519 key, publish its *public* key through controlled device trust configuration, define signer authorization/approval/audit, and exercise rotation/revocation. No Production private key was generated or committed.

For isolated QA only, `qa-p38f-ed25519-20260913` is an Ed25519 key stored with restricted permissions outside Git and outside both installed runtimes at `/private/tmp/digital-observer-push38f-qa-custody/`. Public SPKI is in `/Volumes/DIGITAL_OBSERVER/QA-Releases/PUSH-38F/qa-trust-registry.json`. The QA signer verified the candidate register, SHA-256, sizes, profile/architecture and archive member names, then copied exact bytes to restricted external-volume QA release storage and signed PUSH 19 manifests. This is **not Production custody** and the `qa.invalid` manifest URLs are non-deployable QA references. The external volume is more durable than `/private/tmp`, but is not a remote/offsite backup.

The manager now requires a signed manifest, verified artifact, profile/platform/channel/config compatibility, an installed-runtime verifier, staged baseline runtime and health gate before an atomic bootstrap pointer is committed. A repeat bootstrap is idempotent for the same release; conflicting or missing artifacts fail. The agent no longer invents a trusted empty slot from `development`/`unknown`. Rollback verifies the signed previous slot and artifact again before switching; update slots retain the full signed manifest. The existing isolated tiny-artifact OTA QA passes Connector and Gateway good-update/bad-update rollback, tamper/untrusted/profile negatives and bootstrap-failure checks. **This does not yet prove managed rollback of the two captured 135/147 MB legacy artifacts with a real service switch, fixture identity/config/queues and health restart.** No live bootstrap adapter or trusted-key distribution has been installed. These are blocking internal gates.

The installed Connector is ad-hoc signed (no Team ID) and strict verification fails because sealed resources were added/modified after signing: an embedded backup, `managed-device-auth.mjs`, `run-software-connector.mjs`, and `software-connector-cloud.mjs`. The QA builder now signs nested Mach-O after `install_name_tool` mutation, verifies the final bundle and staged copy strictly, and refuses Production release class without an explicit signing identity. The full QA package build **passed** strict `codesign --verify --deep --strict`; its DMG SHA-256 is `d2a2e130348a270277042ea80a3edf105eba20f807b984997d6e5731b02465a1` and the QA-only DMG/status are preserved under the restricted release-QA store. It has **not** been authorized as a PUSH 19 Ed25519 remediation release, nor tested through a service-manager update; the temporary build tree was removed after preservation. Apple Developer ID distribution certificate, secure certificate custody and notarization credentials are external gaps; no notarization was claimed.

Both **actual captured baseline archives** passed isolated signed bootstrap, repeated bootstrap, controlled synthetic bad-update rollback, baseline artifact hash/parse and fixture identity/config/queue/source-map preservation. This is a manager/slot drill, **not a LaunchAgent/service-start drill**; the QA service-manager restart and full remediation upgrade remain unproved. No signed PUSH 38 remediation runtime release is established yet. Thus the signed QA baseline candidates are **not live KNOWN_GOOD**, and there are no live `CURRENT_RELEASE`/`KNOWN_GOOD_RELEASE` pointers. The machine-readable QA ledger is `DIGITAL_OBSERVER_PUSH_38F_QA_EVIDENCE.json`. Continue isolated proof; keep PR #28 draft/unmerged.

Result: **NOT READY FOR PRE-SOAK.** PUSH 38 remains NOT DONE; PR #28 remains draft/open; V8 and PUSH 39 are not started.

## Completed without changing live components

- Inventoried installed Gateway/Connector runtime, service commands, data/secrets boundaries and runtime dependencies. Both live endpoints still report `development` / build `unknown` / legacy health contract; latest read-only status was Gateway 10/10 and Connector 1/1 progressing, with six unassigned DVR slots and zero stalled relays.
- Captured restricted, code-only legacy artifact candidates and SHA-256 identifiers for each profile; see `DIGITAL_OBSERVER_PUSH_38_KNOWN_GOOD_BASELINES.md` and candidate JSON. No camera credential, private key, queue, Site/source binding or live configuration was packaged.
- Extracted candidates in isolated QA, verified included files against the installed source, parsed code and started each restored `server.mjs` briefly on a separate loopback port with **no real identity/config or sources**. Both returned the expected legacy health response. The temporary extracted copies were removed; compressed candidates are intentionally retained in restricted temporary storage pending signer review or disposal.
- Checked the original and extracted Connector app with macOS strict code-signature verification. **Both failed** (`a sealed resource is missing or invalid`). This is an existing packaging defect, not introduced by archive extraction; it blocks claiming a trusted macOS release from this copy.
- Confirmed the generic OTA manager's `initializeKnownGood()` can create a trusted pointer from caller-provided `development`/`unknown` metadata and an empty slot. That method cannot safely bootstrap these legacy live installations without verified artifact/slot binding. No pointer was created.

## Blocking gates

1. No authorized Ed25519 release signer, signed baseline manifests, trusted live verification-key rollout, or release artifacts were established. An unsigned SHA-256 digest is integrity identification, **not** release authorization.
2. Connector app code signing is invalid even before capture. The baseline package excludes embedded historical backups, so it is a functionally selected runtime candidate, not a byte-for-byte full app image.
3. No complete isolated managed rollback drill for either profile; the limited restored health test has zero cameras and no real/fixture managed identity.
4. Installed components have no active OTA agent/state, no verified known-good slot, and no safe adapter that can switch the current legacy runtime without touching persistent state. Bootstrapping either live component now would violate the stated do-not-fake-known-good rule.

Therefore Gateway bootstrap = **NOT ATTEMPTED**, Connector bootstrap = **NOT ATTEMPTED**, known-good pointers = **ABSENT**, remediation deployment = **NOT ATTEMPTED**, 60-minute pre-soak = **NOT STARTED**, V8 = **NO**. No false PASS/zero-loss/duplicate claims are made for an unrun interval. Do not merge PR #28.
