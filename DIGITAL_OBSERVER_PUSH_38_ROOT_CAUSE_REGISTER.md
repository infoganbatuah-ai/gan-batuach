# PUSH 38 v7 root-cause register

| ID | Severity | Evidence / cause status | Reproduction and fix | v8 acceptance |
| --- | --- | --- | --- | --- |
| R1 | HIGH internal | Confirmed code defects: `ensureRelay` ignored `relayRecovery.next_retry_at`, so requests could bypass bounded backoff; `/health` cleared failure history after a momentary progressing playlist; `code === 0` reset failures even for a rapidly ending native stream. Contributes to DVR/Tapo churn; **not proven sole cause** of either path's outages. | Before: code path inspection and deterministic policy test of repeated failures. Fix: enforce backoff on request, reset only after 60s stable relay, count short-lived normal exits, retryable 503 while recovering. After: policy QA passes; live deployment/soak **not yet verified**. | No request-driven restart storm; count recovery attempts and stability per source. |
| R2 | HIGH unresolved | DVR shared relay loss, 2,012 starts/1,900 socket errors/40 session failures, 18 failure windows. Exact recorder-vs-LAN-vs-host origin not identifiable with v7 per-relay/log data. | Add per-channel progression and timestamped upstream/session/recovery diagnostics; safe isolated session-interruption repro. No claim of full fix yet. | 10/10 continuous progression, bounded sessions/recovery, zero recurring shared windows. |
| R3 | HIGH unresolved | Tapo 27 windows/77 bad samples, including repeated :30 clustering and 146 upstream failures. Log says network unreachable but cannot prove camera/Wi-Fi external origin. | Backoff fix applies, but independent RTSP/network/session/Connector health telemetry and isolated loss/recovery replay still needed. | 1/1 progression without recurrent flapping; external attribution only with matching network evidence. |
| R4 | HIGH unresolved | Connector `/health` unusable in 40 samples across 18 windows while PID stable. v7 did not retain error code/latency/body; non-response vs HTTP/invalid-body cannot be distinguished. | New monitor records response category and latency; service event-loop/HTTP probe instrumentation and deterministic timeout reproduction remain. | All checkpoints classify product vs monitor failure; no unexplained unusable health checks. |
| R5 | HIGH monitoring-only | 1,418/1,440 checkpoints and three >150s gaps. Old loop serialized deep probes and slept after work; process inspection unbounded. | Anchor cadence to start; run deep probes off the minute loop; time-bound process inspection. Monitor accuracy QA passes; 60m real gate pending. | 60m pre-soak has 60/60 on-time checkpoints, no coverage gaps; v8 full 1,440/1,440. |
| R6 | MEDIUM monitoring-only | v7 per-camera availability falsely inferred progression from input presence; missing response was counted as Tapo camera down. | Record per-input `progressing`; report unknown when absent; add component status and PASS/PRODUCT_FAILURE/INSUFFICIENT_EVIDENCE classification. QA passes; live validation pending. | Per-camera availability measured directly and unknown evidence separately blocks qualification. |
| R7 | MEDIUM dependent | One AI deep-probe failure on Tapo coincided with relay absence; no evidence of independent queue/worker stall. | Preserve eligible-work vs intentionally quiet distinction; probe camera→candidate→job→worker→result on next run. | No unexplained AI progress failures; no false stall for quiet/disabled policy. |

No issue is classified `EXTERNAL/ENVIRONMENTAL` on current evidence. Internal HIGH/CRITICAL **confirmed fixed in code: 1/1 (R1)**; HIGH unresolved **R2–R4**; monitoring HIGH **R5** fixed in code but not live-qualified. These categories must not be conflated with a clean v8 start gate.
