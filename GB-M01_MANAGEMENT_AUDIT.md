# GB-M01 — ביקורת מערכת הניהול של גן בטוח

תאריך: 2026-09-07
תחום: מערכת ניהול גן בטוח בלבד. התצפיתן הדיגיטלי נבחן כתלות חיצונית בלבד; לא בוצע שינוי בליבת התצפיתן.

## מסקנה מנהלית

ממשקי מצלמות קיימים עבור מנהלת/בעלת גן, צוות, מפקח והורה. מנגנוני ההרשאה המקומיים, הנפקת צפייה זמנית ותיעוד גישה קיימים בקוד. עם זאת, אין בסיס ראייתי לסמן מצלמות או מסקנות AI כפעילות ייצור דרך התצפיתן: הגדרת המוכנות המקומית מחזירה כיום אינטגרציית גן בטוח `disabled`, מצלמות לכל היותר `readiness`, ו-AI `mock` או `sandbox`, עם `shadowOnly` ודרישת בדיקה אנושית.

לכן, במצב הנוכחי אפשר להציג הכנה, סטטוס, תצורות הרשאה ומסכי "ממתין לחיבור"; אין להציג צפייה חיה, אירוע בטיחות או תובנת AI כתכונת ייצור של גן בטוח רק בגלל שקיים מסך או נתיב קוד.

## מפת יכולות לפי קהל

| קהל | ממשק ניהול קיים | הרשאה בצד גן בטוח | חוזה/ספק תצפיתן | אימות ייצור | סיווג אמת נוכחי |
| --- | --- | --- | --- | --- | --- |
| מנהלת / בעלת גן | דף מצלמות, הגדרה, בדיקת מצב והרשאות | שיוך לגן | חוזה מקומי ושכבת Gateway קיימים | לא נמצא אימות E2E | UI וחוזה קיימים; ספק/ייצור לא מאומתים |
| צוות | גלריית מצלמות מאושרות | שיוך לגן ו-`staff_view_allowed` | תלוי במקור צפייה מאובטח | לא מאומת | UI קיים; זמינות תלויה ב-Gateway |
| מפקח | גלריית מצלמות בגנים משויכים | שיוך מפקח, מדיניות וסיבת צפייה לפי צורך | תלוי במקור צפייה מאובטח | לא מאומת | UI והרשאה קיימים; צפייה אמיתית לא מאומתת |
| הורה | מסך צפייה מאובטחת ומסכי מצב | קשר ילד-גן, מדיניות פעילה, שעות, MFA, נוכחות והרשאת מצלמה | אין ירושת הרשאת הורה מחוזה התצפיתן | לא מאומת | UI והרשאה מקומית קיימים; אין הבטחת Runtime |
| אירועי AI | מסכי אירועים/סקירה קיימים | מיועדים לניהול ולבדיקה אנושית | הנתיב המקומי יוצר במפורש `local_mock`/`shadow` | לא | mock/shadow בלבד; אסור כמסקנת בטיחות |
| התאמת פנים / איסוף | מסך ונתיב review קיימים | מוגבל למנהלת/בעלים/מנהל | הנתיב יוצר במפורש תוצאת mock | לא | לא יכולת זיהוי ילד; אין שחרור אוטומטי |
| ראיות קליפ/תמונה | נתיב מדיה פרטי קיים | בדיקת משתמש והרשאת אתר | חתימה פרטית זמינה כבסיס | אימות ייצור לא נבדק במסגרת זו | חוזה/בסיס קיים; לא לסמן COMPLETE |

## ראיות קוד מרכזיות

- `app/dashboard/parent/cameras/page.tsx`: מסך הורה מציג מצב ורק פותח נגן לאחר אימות.
- `app/dashboard/staff/cameras/page.tsx` ו-`app/dashboard/inspector/cameras/page.tsx`: הגבלת מצלמות לפי שיוך, מדיניות ותפקיד.
- `app/dashboard/garden/cameras/page.tsx`: ממשק ניהול מצלמות; הטקסט עצמו מציג מצב "מוכן לחיבור Gateway" כשהשירות אינו מוגדר.
- `lib/domain/video-streaming.ts`: בדיקות תפקיד, שיוך, מדיניות הורים, MFA, שעות ונוכחות; TTL קצר, watermark, יומן גישה וחסימת RTSP/כתובות פנימיות בדפדפן.
- `lib/domain/digital-observer/service-readiness.ts`: מצב שירות קנוני — אינטגרציית גן בטוח כבויה, מצלמות אינן live ו-AI הוא shadow-only.
- `app/api/ai-camera-events/route.ts`: אירוע מקומי מוגדר במפורש `local_mock`, `shadow`, `requires_human_review` ו-`parent_visible: false`.
- `app/api/garden/face-match-results/route.ts`: תוצאת התאמה נוצרת כ-mock, ללא החלטה ביומטרית וללא שחרור אוטומטי.
- `app/api/digital-observer/event-clips/[id]/media/route.ts`: בסיס למדיה פרטית חתומה, עם בדיקת משתמש/אתר והחזרת no-store.
- `app/api/digital-observer/integration/v1/sites/[id]/status/route.ts`: חוזה סטטוס קיים אך כבוי כברירת מחדל, ודורש לקוח פעיל, token וסביבות מוגדרות; הוא מצהיר במפורש שגישת הורה אינה נובעת ממנו.

## פערים ודרישות אינטגרציה

### DIGITAL OBSERVER INTEGRATION REQUIREMENT — חוזה יכולות אמין

התצפיתן צריך לחשוף חוזה גרסאי, מאומת ומבוקר לכל אתר/מצלמה, עם סטטוסים נפרדים: `ui_available`, `contract_available`, `provider_configured`, `production_verified`, `source_provenance`, `mock_or_shadow`, ו-`unavailable`. גן בטוח לא יסיק סטטוס משדה UI או מנוכחות רשומה.

### DIGITAL OBSERVER INTEGRATION REQUIREMENT — הנפקת צפייה וראיות

נדרש ממשק שרת-לשרת להנפקת צפייה/ראיה פרטית, קצרת-חיים וחתומה, לאחר שגן בטוח השלים את בדיקות ההרשאה שלו. החוזה חייב להחזיר מקוריות, זמן תפוגה, מצב מצלמה והוכחת אימות Runtime; אסור להחזיר URL ציבורי ישיר.

### DIGITAL OBSERVER INTEGRATION REQUIREMENT — אירועים ורמת ודאות

נדרש feed של אירועים שעברו בדיקה, הכולל provenance, מקור מודל, מצב review, קישור ראיה חתום ומצב מחזור חיים. כל פלט mock/local-shadow/synthetic ייחסם מהסלמה, מדוחות בטיחות וממסכי הורים.

### DIGITAL OBSERVER INTEGRATION REQUIREMENT — מסגרת הרשאות חוצת מוצר

נדרש חוזה שמקבל מגן בטוח את מזהה המשתמש, תפקידו, גן, מטרת גישה ומשך סשן; התצפיתן יאשר רק את היכולת הטכנית. גן בטוח יישאר מקור ההחלטה עבור הורה, צוות, מפקח ומנהלת. אין ירושת הרשאות הורה.

## החלטות מוצר מחייבות להמשך GB-M01

1. המצלמות יופיעו בדשבורדים כ"זמינות לצפייה" רק כאשר `production_verified` מתקבל עבור המצלמה והנפקת צפייה חתומה מצליחה בפועל.
2. כל אזכור AI בדשבורד יכלול provenance ומצב review; ב-shadow/mock יוצג כבדיקה פנימית בלבד, ללא ניסוח של אירוע בטיחות.
3. אין לזהות ילד מסוים מתוך person/track/face detection. מסך איסוף נשאר תהליך אנושי, ללא החלטה ביומטרית או שחרור אוטומטי.
4. כל פתיחת מצלמה או ראיה תמשיך להיות כפופה להרשאות גן בטוח ולתיעוד גישה, גם לאחר חיבור התצפיתן.
5. לפני סימון יכולת כ-COMPLETE נדרשת בדיקת ייצור מתועדת של מצלמה אמיתית, נגן מאובטח, תוקף קישור, הרשאות לכל תפקיד, זרימת ראיה ומניעת הצגת mock/shadow.

## מגבלת הביקורת

זוהי ביקורת סטטית של מערכת הניהול ושל החוזים המקומיים. לא בוצע חיבור לחומרת מצלמה, לא נבדקה סביבת ייצור, ולא בוצע audit חדש של ליבת התצפיתן הדיגיטלי.
