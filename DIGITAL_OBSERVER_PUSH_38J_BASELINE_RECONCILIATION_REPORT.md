# DIGITAL OBSERVER — PUSH 38J live Gateway baseline reconciliation

Date: 2026-09-13. PUSH 38 remains **NOT DONE**. This work made no writes to either live camera runtime, device state, service, or trust root. PR #28 remains draft and unmerged; pre-soak and v8 have not started.

## Discrepancy and provenance

The only changed member is `services/video-gateway/journal-loop.mjs` relative to the Gateway runtime root. The old signed baseline member was 21,548 bytes, SHA-256 `1c3013914b8a12ab3dad45f44559af245e0cfc166822af97eabf5a17a8a11151`; the live member is 21,646 bytes (+98), SHA-256 `d53d531be773c3e7948b6c400f1d89142f36c6b5d636acf972af10fc2e58a169`, with filesystem modification time 2026-09-13 04:29:51 +03:00. It is **runtime code** and correctly belongs in the signed release boundary. The change adds a 10-second reuse window to the existing `/cloud/event-manifest` refresh; no new destination, executable, or embedded secret was observed.

The modification is attributable to the separately authorized Supabase-egress remediation task `01a06e1d-e09e-7881-a9e8-ec47f031a9d4`. Its recorded installer invocation produced `installed:true`, the exact new member SHA above, and `process_restarted:true`; its saved pre-change member hashes exactly to the old signed baseline member. The Gateway process began after that installation. The script guarded the old byte hash and syntax-checked the replacement. This is an auditable out-of-band QA/pilot hotfix, **not** a committed/signed Production release. No Git source revision is asserted for the live legacy binary. The earlier investigation stopped before finding this provenance; that conservative stop was correct with the evidence then available.

## Corrected exact-runtime capture and authorization

The release boundary was **correct**, not changed to exclude the code edit. The capture utility uses the prior verified signed archive's 491-member file list; it rejects any additional release-code file, unexpected root entry, symlink/nonregular member, secret/state path, or non-whitelisted content difference. It requires the exact old and new member hashes and exact 10-second code delta, captures from the live runtime read-only, re-extracts the archive, and verifies all member hashes against the live files again. Identity, credentials, source/Site configuration, durable queues, logs and temporary files remain outside the artifact. The external service definition and Node v24.16.0 compatibility were inventoried, not copied into the rollback slot.

Captured 2026-09-13T02:24:57.779Z: `legacy-gateway-91bf6814075f`, Darwin/arm64, 491 regular files, archive SHA-256 `91bf6814075f74e703cbc0b85d30673237531247ec46633c54576d5a4627144d` (135,679,861 bytes). This was a candidate, not known-good. It was then verified, scanned, and authorized as QA release `qa-legacy-gateway-91bf6814075f`, signed with the existing external QA Ed25519 signer `qa-p38f-ed25519-20260913`. The signing private key and archives are outside Git in a restricted controlled QA release store; no Production custody claim is made. A scanner flagged two generic code-variable matches in the archive; both were reviewed as false positives, not embedded private keys or credentials.

Connector was **not** recaptured or resigned. Its previously authorized live baseline `qa-legacy-connector-ee82c20a77ac` still matches its 250 archive members, SHA-256 `ee82c20a77acb7fd8caf692682569ad53581e9c057b11724845c6d947c5a982a`.

## Isolated managed restore and rollback

In isolated launchd-managed QA, the new signed Gateway baseline installed and started as the managed legacy runtime. The installed OTA agent automatically discovered a signed controlled crash release; after bounded persistent failure it quarantined that release and rolled back without a manual updater invocation. The restored artifact hash was **exactly** `91bf6814075f74e703cbc0b85d30673237531247ec46633c54576d5a4627144d`, and health returned. The agent subsequently installed the final PUSH 38I remediation release, then a second controlled crash release rolled back to that promoted known-good remediation hash `c202aafe65ac7f3cf14e8bc51b9bdfcb84702fa1b1de2629fc03685af316e028`. Identity/config/queue fixtures persisted, and the QA runtime never touched live Home. This proves the new baseline works as an exact managed rollback target; it does **not** prove the remediation fixes the real v7 reliability faults.

## Final read-only gate and limits

The final bootstrap planner returned **compatible=true, write_operations=0**, with Gateway 491/491 matching, Connector 250/250 matching, no changed or missing members, and no service/slot-path conflicts. Both legacy services were registered and running. The protected trust root is not yet installed on live Home, as expected before bootstrap. Live filesystem contents could change again; a later operator must repeat hash and health checks immediately before any separately authorized bootstrap.

Gateway `/health` returned HTTP 200, 10/10 connected assigned DVR channels, six unassigned, zero stalled, and ready authorization. Connector's service and listening socket remained present, but an initial bounded sequence of `/health` and `/health/live` probes timed out. A later read-only retry returned HTTP 200 in 6 ms, with **1/1 Tapo connected, zero stalled, ready authorization**. No service was restarted or other live intervention made. The timeouts remain a real transient health-response observation for the later remediation/pre-soak to diagnose; they are not hidden or counted as 60-minute stability proof. Playback and AI were not verified by this filesystem/health gate.

**PUSH 38J result: READY FOR A SEPARATELY AUTHORIZED CONTROLLED QA/PILOT LIVE BOOTSTRAP**, contingent on rechecking both live hashes and health immediately beforehand. Exact Gateway provenance, signed current baseline, isolated restore/rollback, and zero-write two-device dry-run pass; the latest point-in-time Home health check passes. There are zero unresolved **release-baseline/lifecycle** HIGH/CRITICAL defects. The intermittent old Connector health response and other v7 reliability issues remain subject to live remediation and subsequent 60-minute/24-hour qualification; they are not declared fixed. Production signing custody, Apple Developer ID distribution signing, and notarization remain external commercial/distribution gaps. No live bootstrap, pre-soak, v8, PUSH 39, or PR merge is authorized by this report.
