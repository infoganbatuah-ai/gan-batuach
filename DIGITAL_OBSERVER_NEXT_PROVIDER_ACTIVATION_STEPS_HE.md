# התצפיתן הדיגיטלי - הצעדים הידניים שנותרו להפעלת ספקים

## תמונת מצב מעודכנת - 21.8.2026

| תחום | מצב | ראיה | הפעולה הבאה |
|---|---|---|---|
| Google Places (New) | **הושלם** | `GOOGLE_MAPS_PLATFORM_API_KEY` קיים ב-Vercel ל-Production ול-Preview; בוצעה פריסת Production חדשה והמפתח מוגבל ב-Google ל-Places API (New) בלבד | בדיקת כתובת אמיתית אחת במסלול ההרשמה, ללא שמירת כתובת פרטית במסמך QA |
| SMTP של Supabase Auth | **מוגדר ופעיל** | Custom SMTP מסומן כפעיל בפרויקט `gan-batuah`, דרך `smtp.gmail.com`; תבניות הקוד והקישור כבר הוגדרו ובדיקת הרשמה סינתטית קודמת יצרה `Confirmation sent at` | לבצע בדיקת Inbox אחת עם כתובת סינתטית חדשה רק לאחר אישור מפורש ליצירת משתמש ושליחת מייל |
| Supabase / מסד נתונים | **הושלם לבדיקת דמו** | מיגרציית מועמדי האנשים הוחלה בהצלחה; `npm run qa:digital-observer-product` עבר 41/41 עם כניסת משתמש ביתי ועסקי, הפרדת RLS, retention, מועמדים וחבילות | אין שינוי ידני נדרש לפני בדיקת Gateway; אין בכך אישור לוידאו פרטי אמיתי |
| Vercel / Production | **הושלם** | פריסת `f8152ca` במצב `Ready` ומסומנת `Current`; `https://gan-batuach.vercel.app/api/health` מחזיר `app: ok` ו-`supabase: ok` | אין redeploy נוסף נדרש עד שינוי קוד או משתני Gateway |
| Camera Gateway | **טרם הוגדר - דורש החלטה ואישור** | אין ב-Vercel `VIDEO_GATEWAY_URL`, `DIGITAL_OBSERVER_CAMERA_GATEWAY_URL` או סוד חתימה תואם | לבחור Gateway, סביבת הרצה ומצלמת בדיקה סינתטית/לא פרטית; רק אז להגדיר סודות ולבצע smoke test |
| AI Shadow | **חסום על ידי Gateway** | אין מקור frame מאובטח אמיתי ואין ספק inference פעיל | לבחור ספק/Worker רק אחרי מעבר בדיקת Gateway; להתחיל Shadow עם ביקורת אנושית בלבד |
| Email תפעולי / Push / SMS / WhatsApp / שיחה | **לא פעילים** | SMTP הנוכחי מטפל באימות Supabase בלבד; ספקי ההתראות של המוצר נשארים Mock/Readiness | לבחור ספקי Sandbox בנפרד. אין לשלוח Production ללא אישור נפרד |
| תשלומים וחירום | **חסומים בכוונה** | לא הופעל ספק חי ולא אושרה מדיניות escalation | לא לבצע בשלב הנוכחי |

> לא נוצרו סודות חדשים, לא הועברו ערכי סוד, לא הופעלו תשלומים, מצלמות חיות, ביומטריה או שיחות חירום במסגרת בדיקה זו.

השוואת הספקים, הארכיטקטורה המשותפת והמלצת הבחירה מרוכזות ב-`DIGITAL_OBSERVER_GATEWAY_PROVIDER_DECISION_HE.md`.

## מה כבר מוכן

- מסך כתובת מלא: עיר, רחוב, בניין, דירה, קרקע/קומה ומספר קומה.
- בסיס למפה ולסטטיסטיקה אזורית: קווי רוחב/אורך נשמרים רק לאחר אימות ספק.
- התחלת חלון איסוף של 30 יום לאחר הוספת מצלמה; הזמן אינו מוצג כהוכחת אמינות.
- מסך "התצפיתן שלי" עם מצלמות, מצב מנוע, תובנות בזמן אמת ממידע קיים, שיחה, מחזור שיפור מדיד ומשוב "הכול בסדר".
- מסך אנשים שכיחים שמקבל מועמדים ממנוע AI עתידי ודורש אישור והסכמה לפני הפיכתם לאנשים מוכרים.
- הפרדת פרטיות לעסק המטפל בילדים: שלד ותנועה בלבד, ללא זיהוי פנים.
- מורשי עדכונים ושני מכשירים לכל אתר.
- בדיקת QA אוטומטית מלאה: 41 מתוך 41 עברו.
- בדיקת מסכים: 24 צילומים ביתי/עסקי במובייל, טאבלט ודסקטופ ללא overflow.

## מה דניאל צריך לעשות איתי, לפי הסדר

### 1. חיבור כתובות אמיתיות - הושלם ברמת ספק ופריסה

מפתח Google Maps Platform ל-Places API (New) כבר נוצר, הוגבל ונשמר ב-Vercel בשם:

`GOOGLE_MAPS_PLATFORM_API_KEY`

אין לשלוח את המפתח בצ'אט ואין לשמור אותו בקוד. נשארה בדיקת מסך אחת שבה דניאל בוחר כתובת ומוודא שעיר, רחוב, מספר ומיקום מסתנכרנים. את הכתובת הפרטית עצמה אין להעתיק לדוח.

### 2. חיבור מצלמה אחת דרך Gateway - הפעולה הטכנית הבאה, טרם אושרה

נבחר מצלמת בדיקה ללא מידע פרטי אמיתי, נתקין/נגדיר Gateway מקומי, ונשמור את פרטי החיבור בצד השרת בלבד. נבדוק:

- חיבור וניתוק;
- preview מאובטח;
- token זמני לצפייה;
- health ו-last seen;
- אין RTSP או סיסמה בדפדפן.

לפני ביצוע נדרשת החלטה מפורשת על שלושה פריטים:

1. היכן ירוץ ה-Gateway: מחשב מקומי/מיני-PC באותה רשת, שרת Edge ייעודי או שירות ענן בעל גישה מאובטחת לרשת.
2. איזה מקור בדיקה לא-פרטי ישמש: מצלמת בדיקה ייעודית, NVR/DVR במעבדה או stream סינתטי.
3. האם להתחיל ב-ONVIF, ב-RTSP או בחיבור NVR/DVR.

לאחר הבחירה Codex יכול להכין את קובצי ההגדרה ובדיקת החיבור. ערכי סוד יוזנו ישירות ב-Vercel/Gateway ולא בצ'אט או בקוד.

### 3. AI Shadow - חסום עד מעבר Gateway

נחבר ספק inference או worker אמיתי במצב Shadow בלבד. בהתחלה הוא ייצור הצעות לבדיקה אנושית ולא פעולות אוטומטיות. נבדוק אדם, בעל חיים, כניסה/יציאה, מצלמה מכוסה ותנועה מחוץ לשעות.

### 4. התראות - SMTP אימות הושלם; ספקי המוצר עדיין Sandbox/Mock

SMTP של Supabase Auth כבר מוגדר ושולח הודעות אימות. זה אינו ספק ההתראות התפעוליות של התצפיתן. את ספקי המוצר נחבר לפי הסדר: דוא"ל, Push, SMS, WhatsApp ושיחה. כל ספק יתחיל ב-Sandbox/בדיקה, עם תיעוד הצלחה, כישלון וניסיון חוזר. לא תישלח הודעת production לפני אישור נפרד.

### 5. חירום

רק לאחר כתובת מאומתת, ספק שיחה, נוסח מאושר ובדיקת נוהל אנושי ניתן יהיה לבחון escalation. כרגע אין שיחה אוטומטית למוקד חירום, וזה מכוון.

### 6. אפליקציה

לאחר שינויי המובייל יש להריץ `npx cap sync`, לבדוק Push במכשיר אמיתי ולוודא שמגבלת שני המכשירים עובדת ב-iOS וב-Android.

## הפעולה הבאה המדויקת

Google Places, SMTP, המסד והפריסה כבר מוכנים לבדיקת דמו. הפעולה הבאה דורשת החלטה ולא הגדרה עיוורת: לבחור סביבת Gateway ומקור מצלמת בדיקה לא-פרטי. לאחר שדניאל יאשר את שלושת הפריטים בסעיף 2, ניתן להגדיר Gateway במצב בדיקה, לבצע preview מאובטח ו-health check, ורק לאחר הצלחה לעבור ל-AI Shadow.

מפת תנאי הקבלה להפעלה רציפה, AI Shadow, כיול ומשוב נמצאת ב-`DIGITAL_OBSERVER_24_7_RELIABILITY_ROADMAP_HE.md`.

בדיקת מייל נוספת אינה חסם ל-Gateway. אם רוצים לאמת שוב את המסירה לתיבה, יש לאשר במפורש יצירת משתמש סינתטי חדש ושליחת הודעת אימות אחת; אין צורך לשנות שוב את הגדרת ה-SMTP.
