# התצפיתן הדיגיטלי - הצעדים הידניים שנותרו להפעלת ספקים

## מה כבר מוכן

- מסך כתובת מלא: עיר, רחוב, בניין, דירה, קרקע/קומה ומספר קומה.
- בסיס למפה ולסטטיסטיקה אזורית: קווי רוחב/אורך נשמרים רק לאחר אימות ספק.
- התחלת מסלול למידה של 30 יום לאחר הוספת מצלמה.
- מסך "התצפיתן שלי" עם מצלמות, מצב למידה, תובנות, כללים ומשוב "הכול בסדר".
- הפרדת פרטיות לעסק המטפל בילדים: שלד ותנועה בלבד, ללא זיהוי פנים.
- מורשי עדכונים ושני מכשירים לכל אתר.
- בדיקת QA אוטומטית מלאה: 35 מתוך 35 עברו.

## מה דניאל צריך לעשות איתי, לפי הסדר

### 1. חיבור כתובות אמיתיות

נדרש מפתח Google Maps Platform ל-Places API (New). המפתח יישמר ב-Vercel בשם:

`GOOGLE_MAPS_PLATFORM_API_KEY`

אין לשלוח את המפתח בצ'אט ואין לשמור אותו בקוד. לאחר הוספתו נבדוק כתובת אמיתית ונאשר שעיר, רחוב, מספר ומיקום מסתנכרנים.

### 2. חיבור מצלמה אחת דרך Gateway

נבחר מצלמת בדיקה ללא מידע פרטי אמיתי, נתקין/נגדיר Gateway מקומי, ונשמור את פרטי החיבור בצד השרת בלבד. נבדוק:

- חיבור וניתוק;
- preview מאובטח;
- token זמני לצפייה;
- health ו-last seen;
- אין RTSP או סיסמה בדפדפן.

### 3. AI Shadow

נחבר ספק inference או worker אמיתי במצב Shadow בלבד. בהתחלה הוא ייצור הצעות לבדיקה אנושית ולא פעולות אוטומטיות. נבדוק אדם, בעל חיים, כניסה/יציאה, מצלמה מכוסה ותנועה מחוץ לשעות.

### 4. התראות

נחבר לפי הסדר: דוא"ל, Push, SMS, WhatsApp ושיחה. כל ספק יתחיל ב-Sandbox/בדיקה, עם תיעוד הצלחה, כישלון וניסיון חוזר. לא תישלח הודעת production לפני אישור נפרד.

### 5. חירום

רק לאחר כתובת מאומתת, ספק שיחה, נוסח מאושר ובדיקת נוהל אנושי ניתן יהיה לבחון escalation. כרגע אין שיחה אוטומטית למוקד חירום, וזה מכוון.

### 6. אפליקציה

לאחר שינויי המובייל יש להריץ `npx cap sync`, לבדוק Push במכשיר אמיתי ולוודא שמגבלת שני המכשירים עובדת ב-iOS וב-Android.

## הפעולה הבאה המדויקת

הפעולה הידנית הראשונה היא יצירה והגבלה של מפתח Google Maps Platform. לאחר מכן Codex יכול להוסיף אותו ל-Vercel, לפרוס מחדש ולבדוק את מסע הכתובת מההרשמה עד "התצפיתן שלי".
