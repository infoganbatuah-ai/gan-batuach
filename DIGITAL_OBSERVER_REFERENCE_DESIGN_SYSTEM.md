# תצפיתן דיגיטלי - מערכת העיצוב המחייבת מתמונות הייחוס

תאריך: 22.08.2026
תחולה: מסלולי משתמש ביתי, משתמש עסקי, הרשמה, מצלמות, אירועים, הקלטות, אנשים מוכרים, מנוי והגדרות.
מקור אמת: שבע תמונות הייחוס שמסר דניאל. המסמך מתעד את הכללים שהוטמעו; הוא אינו מחליף את התמונות.

## 1. שפה חזותית

| רכיב | כלל מחייב | מימוש |
|---|---|---|
| כיוון | עברית ו-RTL כברירת מחדל; מספרים, זמן ושדות טכניים נשארים קריאים | `app/digital-observer/layout.tsx`, `app/styles/digital-observer-product.css` |
| צבע בסיס | כחול כהה `#061d3a` לניווט ולפעולה מרכזית, טורקיז `#08a99d` לפעילות, לבן ומשטח `#f4f8fa` לתוכן | משתני `--do-*` ב-CSS |
| מצב תקין | ירוק/טורקיז עם טקסט מפורש; לא צבע לבדו | `do-badge good`, סטטוסים שמגיעים מה-runtime |
| אזהרה/חריג | ענבר או אדום, עם הסבר ורמת ביטחון | `do-badge warn/bad`, מרכז אירועים |
| רדיוס | 7-8px לכרטיסים, טבלאות וכלי עבודה; עיגול רק לאווטאר, סטטוס או כפתור אייקון מוכר | רכיבי `do-panel`, `do-camera-card`, `do-icon-button` |
| צל | צל קל להפרדה בלבד; אין כרטיסים צפים בתוך כרטיסים | `--do-shadow-soft` |
| טיפוגרפיה | כותרת מסך 18-31px לפי ההקשר; כותרת פאנל 18-22px; טקסט 12-16px; `letter-spacing: 0` | כללי `do-page-title`, `do-section-head`, `do-panel` |

## 2. מבנה וניווט

- דסקטופ: סרגל ימני קבוע ברוחב 190px, אזור תוכן עד 1260px, סרגל עליון קצר, מידע צפוף אך סריק.
- טאבלט: גריד 1-2 עמודות בהתאם למסך; אין דסקטופ שבור ואין טלפון מתוח.
- מובייל: קומפוזיציה נפרדת, לא דסקטופ מוקטן. כותרת עליונה, מגירת כל המסכים, ניווט תחתון בן חמישה יעדים ופעולה מרכזית עגולה לתצפיתן.
- תוכן במובייל מקבל תחתית בטוחה של 92px ועוד `safe-area`, כך שהניווט אינו מסתיר פעולה.
- כל יעד הקיים בסרגל הדסקטופ נגיש גם ממגירת המובייל.
- כרטיס מצלמה במובייל הוא שורת תמונה+שם+סטטוס; בדסקטופ הוא אריח מדיה צפוף.

## 3. היררכיית מוצר

1. מצב המקום או העסק עכשיו.
2. מצלמות ואירועים שדורשים תשומת לב.
3. פעולה מיידית או שיחה עם התצפיתן.
4. פרטים, דוחות ומדדים.
5. הגדרות טכניות ומדדי תשתית בחלק מתקדם או במסך ייעודי.

הדשבורד אינו מציג מטריצת קיצורים גנרית. כל כרטיס מרכזי מוביל לנתיב אמיתי, מציג נתון אמיתי, או מציג מצב המתנה מפורש.

## 4. התאמת מסלול ביתי ועסקי

| אזור | ביתי | עסקי |
|---|---|---|
| מסך ראשון | שקט/אירועים, מצלמות הבית, אירועים אחרונים ומצב הבית | סטטוס מערכתי, פעילות 24 שעות, אירועים פתוחים ומצלמות פעילות |
| מצלמות | כניסה, סלון, חדרים, חצר; פעולה פשוטה וידידותית | אתרים, מיקומים, בריאות חיבור ומקורות מרובים |
| התצפיתן | שיחה על כניסה, ילד/תינוק, חיית מחמד ושעות שקטות | שיחה על כניסה/יציאה, פעילות מחוץ לשעות, אזור מוגבל ומצב מצלמה |
| אנשים | משפחה ומורשים בהסכמה | צוות והרשאות, מורשי עדכון והפרדת גישה |
| מנוי | חבילות ביתיות בלבד | חבילות עסקיות ו-Enterprise בלבד |

## 5. מצבים ואינטראקציות

כל מסך נתונים חייב לכלול, לפי הצורך:

- `loading`: מסכי `app/digital-observer/loading.tsx` ו-admin loading.
- `error`: מסך שגיאה עם ניסיון חוזר, ללא fallback מזויף.
- `empty`: הסבר מה חסר והפעולה הבאה.
- `readiness`: "מוכן לחיבור", "ממתין ל-Gateway", "Shadow בלבד", או "חיוב מדומה".
- `active`: רק לאחר נתון שרת או חיבור מאושר.
- `permission`: גישה נבדקת בצד השרת וב-RLS; אין כפתור שמתחזה להרשאה.

כפתור אינו נשאר ללא פעולה. הוא מקושר לנתיב, שולח פעולה ל-API עם מצב busy/result, או מוצג כ-readiness עם סיבה.

## 6. כללי מציאות ובטיחות

- אין תווית LIVE ללא stream מאובטח.
- מקור דמו מסומן כהדמיה ואינו מתחזה למצלמה חיה.
- AI חי, ביומטריה, חיוג חירום, SMS, WhatsApp וחיוב אמיתי נשארים כבויים.
- אירוע אלימות, שוד או גניבה מוצג כחשד עם ביטחון וביקורת אנושית.
- אדם מוכר נשמר רק בהסכמה; במקום המטפל בילדים מופעלת מדיניות שלד/תנועה ללא מאגר פנים.
- הקלטה מוצעת להורדה רק כשיש קובץ חתום אמיתי; retention מוגבל ל-48 שעות.
- מספרים, תאריכים ותרשימים נגזרים מה-runtime ומהמסד; אין fallback מספרי קשיח.

## 7. מיפוי הרפרנסים

| תמונה | נתיבים ורכיבים שהוגדרו לפיה |
|---|---|
| דשבורד ביתי | `/digital-observer/dashboard`, `/digital-observer/cameras`, `/digital-observer/alerts`, `/digital-observer/rules` |
| דשבורד עסקי | dashboard עסקי, `/digital-observer/sites`, מצלמות, אירועים, צוות והרשאות |
| הוספת מצלמה ביתי | `/digital-observer/cameras/add`, אשף 4 שלבים, QR, מקור/פרטים/מטרות/סיום |
| הקלטות, אנשים מוכרים, מרכז התראות, פרטי אירוע | `/recordings`, `/people`, `/alerts?event=...` |
| חידוד מובייל והתראות | מעטפת מובייל, ניווט תחתון, מגירה, כרטיסי מצלמה, התראות ואשף מצלמה |
| תשלומים, מנויים וניהול מערכת | `/billing`, `/settings`, ומרכז admin הייעודי |
| רישום והתחברות ביתי ועסקי | `/login`, `/register`, `/verify`, `/onboarding`, הפרדת מסלול לפי החשבון השמור |

## 8. כללי הרחבה

כל מסך חדש חייב להשתמש ב-`ObserverAppShell`, ברכיבי הסטטוס הקיימים ובמשתני `--do-*`. אין להוסיף override גלובלי גדול, צבע חדש דומיננטי, כרטיס מקונן, מדד מומצא או נתיב נפרד למובייל. שינוי משמעותי דורש צילום 390/430/768/1024/1366/1440 ובדיקת overflow, פעולות ו-readiness.
