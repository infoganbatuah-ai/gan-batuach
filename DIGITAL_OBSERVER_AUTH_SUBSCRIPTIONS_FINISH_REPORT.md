# דוח סיום Authentication ומנויים - תצפיתן דיגיטלי

תאריך: 22.08.2026
סטטוס: `AUTH_SUBSCRIPTIONS_E2E_ACCEPTED_NO_LIVE_BILLING`

## סיכום מנהלים

מסלולי הכניסה, האימות, השחזור והמנויים של התצפיתן הדיגיטלי הופרדו ממסלולי גן בטוח ותוקנו ברמת הקוד, ההרשאות והניווט. בוצעו בדיקות אמיתיות מקצה לקצה להרשמה חדשה, מייל אישור, קישור אישור חד-פעמי, כניסה ראשונה ושחזור סיסמה עם חשבונות פיילוט סינתטיים בלבד. בוצעו גם כניסות אמיתיות לחשבון ביתי, עסקי ו-Admin ייעודי לתצפיתן.

לא מופעל חיוב אמיתי. שינוי מסלול יוצר בקשת מוכנות מאומתת בצד השרת בלבד. מצלמות, AI, הודעות וספקים חיים נשארו כבויים.

אין חסם פתוח בזרימות Authentication שנבדקו. חיוב אמיתי, מצלמות, AI, הודעות וספקים חיים נשארים מחוץ לשלב הזה וכבויים בכוונה.

## תקלות השורש שתוקנו

1. קישורי Supabase נשלחו ל-callback עם פרמטרים שלא תמיד תאמו במדויק ל-Allow List, ולכן Supabase היה יכול לחזור ל-Site URL הלא נכון.
2. callback ישן בצד השרת לא טיפל באופן אחיד ב-PKCE, קישור `token_hash`, טוקנים ב-fragment או session קיים.
3. קישור recovery לא שמר תמיד את הקשר המוצרי של התצפיתן ולכן היה יכול ליפול למסלול גן בטוח.
4. הרשאת Observer Admin כללה fallback רחב מדי לתפקיד Admin של גן בטוח.
5. חבילות ביתיות ועסקיות לא נאכפו במידה מספקת גם בצד השרת.
6. זרימת שכחתי סיסמה לא הייתה אחידה ולא הייתה מוגנת במלואה מפני גילוי אם חשבון קיים.
7. תבנית אישור ההרשמה נשמרה ב-Supabase כ-HTML פגום, ושירות Auth המשיך לקרוא עותק cache ישן גם לאחר תיקון התבנית. התבנית הוחלפה בתבנית תקנית המבוססת על `ConfirmationURL` ו-`Token`, ולאחר Restart מבוקר שירות Auth טען אותה ושליחת המייל חזרה לפעול.

## אימות, הרשמה ושחזור

### יושם

- callback חדש בדפדפן ב-`/auth/callback` התומך ב-PKCE, `token_hash`, implicit tokens ו-session קיים.
- אימות ישיר ובטוח ב-`/auth/confirm` עבור הרשמה ושחזור.
- שמירת הקשר המוצרי לזמן קצר בלבד, ולא בתוך סוד או פרמטר לקוח שניתן לסמוך עליו לבדו.
- מסך נייטרלי לקישור שחזור שפג או שכבר נוצל: `/auth/recovery-invalid`.
- מסך שכחתי סיסמה ייעודי לתצפיתן ומסך איפוס משותף ומאובטח.
- סיסמה חדשה מחייבת session אמיתי של Supabase, התאמת שדות ולפחות 10 תווים.
- אחרי שינוי הסיסמה ה-session המקומי נסגר ונדרשת כניסה מחדש.
- תגובה זהה לבקשת שחזור בין חשבון קיים ולא קיים, כדי לא לחשוף קיום חשבון.
- יציאה בטוחה דרך `/api/auth/logout`, עם redirect פנימי בלבד למסלול התצפיתן.
- מחיקת קובצי `page 2.tsx` כפולים כדי למנוע מסלולים עמומים.

### תוצאות בפועל

| זרימה | תוצאה | ראיה |
|---|---|---|
| כניסת משתמש ביתי | PASS | dashboard ביתי אמיתי + צילום 390x844 |
| כניסת משתמש עסקי | PASS | dashboard עסקי אמיתי + צילומי tablet/desktop |
| כניסת Observer Admin | PASS | signed product claim + צילומי mobile/tablet/desktop |
| חסימת משתמש עסקי מ-Observer Admin | PASS | redirect חזרה עם שגיאת הרשאה |
| שכחתי סיסמה | PASS | תגובה אחידה גם לכתובת שאינה קיימת |
| recovery אמיתי במייל | PASS | קישור טרי פתח session והגיע למסך שינוי סיסמה |
| שינוי סיסמה | PASS | Supabase עדכן וה-session המקומי נסגר |
| שימוש חוזר באותו קישור | PASS | הופנה למסך קישור שפג/נוצל |
| הרשמה חדשה + מייל חדש | PASS | חשבון ביתי סינתטי חדש נוצר ומייל אישור הגיע לתיבת הפיילוט |
| שליחה חוזרת של אימות | PASS | Supabase קיבל את הבקשה ושלח הודעה חדשה לאחר חלון האבטחה |
| קישור אישור הרשמה | PASS | הקישור האחרון אושר פעם אחת והגיע ל-`/digital-observer/login` |
| כניסה ראשונה אחרי אישור | PASS | השרת יצר session והפנה ל-`/digital-observer/onboarding?type=home` |

## הפרדה בין ביתי לעסקי

### מסלול ביתי שנבדק

- חשבון בעל `account_type=home` נכנס ל-dashboard הביתי ולא לממשק עסקי.
- dashboard במובייל מציג מצלמות, אירועים שממתינים לבדיקה, סטטוס ניסיון ופעולות ביתיות.
- דף המנוי מציג חבילות ביתיות בלבד.
- פרמטר URL אינו יכול להפוך חשבון ביתי לעסקי.
- חוויית מובייל 390x844 נבדקה ללא overflow אופקי וללא CTA מוסתר.

### מסלול עסקי שנבדק

- חשבון בעל `account_type=business` נכנס לסקירת העסק עם אתרים, מצלמות וכללי ניטור.
- dashboard נבדק ב-820 וב-1280 פיקסלים ללא overflow אופקי.
- דף המנוי מציג חבילות עסקיות ו-Multi-Site, ואינו מציג חבילות ביתיות.
- API המנויים מאמת בעלות על האתר וסוג חשבון, ולא מסתמך רק על הכרטיס שנבחר בממשק.
- פרמטר URL אינו יכול לשנות את מסלול החשבון השמור.

### Observer Admin שנבדק

- הכניסה מבוססת על claim חתום `digital_observer_admin` בלבד.
- תפקיד Admin כללי של גן בטוח אינו מעניק גישת Observer Admin.
- מרכז הבקרה נבדק במובייל, טאבלט ודסקטופ.
- משתמש עסקי רגיל אינו יכול לפתוח את המסלול.
- הממשק מציג מטא-דאטה בטוח ואינו מציג סודות מצלמה, RTSP או פרטי תשלום.

## מנויים ללא חיוב אמיתי

- הבחירה הביתית והעסקית מסוננת לפי סוג החשבון השמור.
- בקשת שינוי מסלול נוצרת רק לאחר אימות session ובעלות על האתר.
- בקשה כפולה בהמתנה נחסמת.
- נשמר סוג שינוי upgrade/downgrade לצורך טיפול עתידי.
- אין קריאה לספק סליקה, אין אישור תשלום מזויף ואין entitlement חי שניתן מהלקוח.
- הטקסט בממשק מבהיר שהחיוב עדיין אינו פעיל ושמדובר בבקשת מוכנות.

## אבטחה והפרדת מוצרים

- אין service role בצד הלקוח.
- אין סיסמאות, קודי OTP או טוקנים בדוחות ובצילומים.
- חומר רגיש מוסר מכתובת הדפדפן אחרי callback.
- redirect של logout מוגבל לנתיבים פנימיים של התצפיתן.
- בדיקות RLS הוכיחו שמשתמש ביתי ועסקי אינם קוראים אתרי, מצלמות, אירועי, אנשים מוכרים, מועמדי זיהוי, קטעים או התראות של החשבון האחר.
- מצלמות חיות, AI חי, הודעות, חיוג חירום וחיוב אמיתי לא הופעלו.

## Supabase ו-SMTP

- SMTP המותאם פעיל עבור פרויקט `gan-batuah`.
- תבנית אישור הרשמה משתמשת ב-`ConfirmationURL` הרשמי של Supabase ומציגה גם `Token` להזנה ידנית.
- תבנית שחזור מפנה ל-`/auth/confirm` עם `token_hash` ו-`type=recovery`.
- מכסת המייל הוחזרה לערך הבטוח 60 לשעה אחרי בדיקות, ולא נשארה מוגדלת.
- הגבלת הזמן בין בקשות resend נשארה פעילה; הבדיקה המתינה לחלון האבטחה ולא עקפה אותו.

## בדיקות וראיות

| בדיקה | תוצאה |
|---|---|
| `npm run typecheck` | PASS |
| `npm run build` | PASS - 470 עמודים סטטיים |
| `git diff --check` | PASS |
| Digital Observer QA מול Supabase/RLS | PASS - 64/64 |
| Visual QA | PASS למסכים שצולמו |
| recovery E2E אמיתי | PASS |
| signup email E2E חדש | PASS |
| signup link -> login -> home onboarding | PASS |

הראיות נמצאות ב-`qa-evidence/digital-observer-auth-subscriptions/`. במסכי השחזור במובייל נמדדו `innerWidth=390` ו-`scrollWidth=390`, ולא נראתה אנימציית מעבר בזמן הצילום.

## קבצים עיקריים

- `app/auth/callback/page.tsx`
- `components/auth/auth-callback-client.tsx`
- `app/auth/confirm/route.ts`
- `app/auth/recovery-invalid/page.tsx`
- `components/auth/password-update-form.tsx`
- `app/digital-observer/forgot-password/page.tsx`
- `app/forgot-password/actions.ts`
- `app/reset-password/page.tsx`
- `app/digital-observer/auth-actions.ts`
- `app/api/auth/logout/route.ts`
- `app/api/digital-observer/auth/login/route.ts`
- `lib/domain/digital-observer/admin-access.ts`
- `lib/domain/digital-observer/access.ts`
- `app/api/digital-observer/billing/route.ts`
- `app/digital-observer/billing/page.tsx`
- `app/digital-observer/onboarding/page.tsx`
- `scripts/qa/check-digital-observer-product.mjs`

## מה עדיין פתוח

1. מסלול קוד ה-OTP קיים ומטפל ב-`email` וגם ב-`signup`, אך בסבב הסופי הופעל קישור האישור החד-פעמי ולא בוצעה הזנה ידנית נפרדת של הקוד.
2. סליקה, מצלמות, Gateway, AI, הודעות וחיוג נשארים מחוץ לשלב הזה וכבויים בכוונה.
3. לפני הפעלת חיוב אמיתי נדרשים ספק סליקה, webhooks, receipt validation ובדיקות sandbox נפרדות.

## המלצה

`AUTH_SUBSCRIPTIONS_E2E_ACCEPTED_NO_LIVE_BILLING`

זרימות ההרשמה, האימות, הכניסה, השחזור והפרדת המנויים עברו בדיקות E2E בפריסה החיה. אין בכך אישור להפעיל חיוב או ספקים חיים; אלה נשארים שלבים נפרדים.
