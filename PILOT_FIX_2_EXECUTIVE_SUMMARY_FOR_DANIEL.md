# PILOT FIX 2 - סיכום מנהלים לדניאל

תאריך: 2026-06-27

## סטטוס קצר

PILOT FIX 2 הושלם כשלב בדיקה ותכנון אבטחת Supabase/RLS, אבל שער האבטחה לפיילוט אמיתי עדיין לא נסגר.

הסיבה פשוטה: מתוך סביבת העבודה הזו לא הייתה גישה ישירה לפרויקט Supabase האמיתי, ולא נמצא Supabase CLI פעיל. לכן אפשר היה לבדוק את הקוד והמיגרציות מקומית, אבל אי אפשר היה להוכיח בפועל שהמדיניות רצה ונאכפת בפרויקט האמיתי.

## מה נבדק

- המיגרציות המקומיות של Supabase.
- מיגרציית הקשחת הורים: `20260616000100_parent_rls_scope_hardening.sql`.
- מיגרציית תשלומים/ספקים: `20260616000200_payment_provider_rls_scope_hardening.sql`.
- מיגרציית PROD 1 לוובהוקים ודמו/הקפאה: `20260627000100_prod1_provider_webhooks_demo_freeze_readiness.sql`.
- עוזרי Supabase בצד שרת/דפדפן.
- שימוש ב־Service Role.
- נתיבי API רגישים.
- לוגיקת signed URL.
- לוגיקת מצלמות וטוקני צפייה.
- לוגיקת AI/Shadow בסיסית.
- הפרדת Digital Observer.

## מה עבר

- `npm run typecheck` עבר.
- `npm run build` עבר.
- `git diff --check` עבר.
- המיגרציות הקריטיות קיימות מקומית.
- לא נמצא שימוש ישיר ב־Service Role בתוך client components.
- העלאת קבצים משתמשת ב־Service Role בצד שרת בלבד ויוצרת signed URL קצר.
- קוד צפייה במצלמות כולל חסימות חשובות: בלי RTSP ישיר, טוקן קצר, בדיקת תפקיד, מדיניות הורים, MFA, נוכחות ילד, שעות צפייה ואודיט.

## מה עדיין לא עבר

לא הוכח מול Supabase האמיתי:

- שהמיגרציות באמת רצות בפרויקט היעד.
- שה־RLS מופעל על כל הטבלאות הרגישות.
- שהורה א' לא יכול לראות ילד ב'.
- שמנהלת גן א' לא יכולה לראות גן ב'.
- שאיש צוות לא מאושר לא רואה ילדים/הורים.
- שמפקח לא משויך לא רואה גנים.
- שמסמכים רגישים לא ציבוריים.
- ש־signed URL לא נוצר לקובץ לא מורשה.
- שהורה/צוות/מפקח לא רואים רשומות ספקי תשלום.
- שהורה לא רואה raw AI events.
- שאין דליפת מצלמות או Digital Observer בין מוצרים/לקוחות.

## מה דניאל צריך להריץ ידנית ב־Supabase

המסמך:

`PILOT_FIX_2_REAL_SUPABASE_MANUAL_VERIFICATION_PLAN.md`

כולל תוכנית צעד־אחר־צעד:

1. לפתוח Supabase SQL Editor בפרויקט היעד.
2. לבדוק שה־RLS מופעל על הטבלאות הרגישות.
3. לבדוק שאין מדיניות רחבה מדי כמו `USING (true)` על מידע רגיש.
4. לבדוק שכל buckets הרגישים הם private.
5. ליצור משתמשים ודאטה סינתטיים בלבד.
6. להריץ בדיקות חיוביות ושליליות לכל תפקיד.
7. לתעד PASS/FAIL לכל בדיקה.

## האם פיילוט אמיתי מותר עכשיו?

לא.

הסטטוס הוא:

`RLS_MANUAL_SUPABASE_VERIFICATION_REQUIRED`

כלומר: אפשר להמשיך להכין משפטית/תפעולית את הפיילוט, אבל אסור להכניס ילדים/הורים/צוות אמיתיים לפני חתימת Supabase/RLS ידנית.

## השלב הבא

השלב הבא המומלץ:

`PILOT FIX 3 - Legal, Privacy, Consent & Child Data Documentation Closure`

אפשר להתקדם אליו במקביל, אבל PILOT FIX 2 נשאר שער חובה לפני כל פיילוט אמיתי.

