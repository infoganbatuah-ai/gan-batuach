# DEMO AUTH SETUP 1 - סיכום לדניאל

## למה AUTHED UX/UI QA 2 נחסם?

כי הצלחנו להתחבר בפועל רק כהורה. לא הייתה דרך בטוחה להחליף סשן ולהיכנס כמנהלת, צוות, מפקחת, אדמין ו-Digital Observer בלי לחשוף סיסמאות או לעשות bypass.

## מה קיים כבר?

נמצאו משתמשי דמו קיימים עבור:

- הורה: `parent.1@demo.ganbatuach.com`
- מנהלת: `manager.rakefet@demo.ganbatuach.com`
- צוות משויך: `staff.1@demo.ganbatuach.com`
- מפקחת משויכת: `inspector.yael@demo.ganbatuach.com`
- אדמין: `admin-demo@demo.ganbatuach.com`

לא הדפסתי סיסמאות ולא שמרתי אותן בדוחות.

## מה עדיין חסר?

- משתמש צוות לא משויך
- משתמש מפקחת לא משויכת
- משתמש Digital Observer מחובר
- סיסמאות דמו זמינות בצורה בטוחה לבדיקה

## מה תוקן עכשיו?

1. יציאה מהמערכת דרך `/api/auth/logout` עובדת עכשיו גם כקישור רגיל, כדי שאפשר יהיה להחליף משתמשים בין תפקידים.
2. סקריפט הדמו המלא כבר לא מדפיס סיסמאות בסוף הריצה.
3. נוצר קובץ דוגמה בטוח: `.env.qa-demo.example`.
4. נוצר סקריפט אופציונלי: `npm run qa:create-demo-role-users`, שמייצר משתמשי QA סינתטיים רק אם אתה מספק סיסמאות מקומית ובוחר סביבת demo/staging/pilot.

## איך ממשיכים?

אפשרות פשוטה:

1. תן ל-Codex/QA את סיסמאות הדמו ידנית בזמן הבדיקה, בלי להכניס אותן לקוד.
2. צור ידנית בסופאבייס את שלושת המשתמשים החסרים.
3. נריץ AUTHED UX/UI QA 3.

אפשרות מסודרת:

1. העתק את `.env.qa-demo.example` לקובץ מקומי שלא נכנס לגיט: `.env.qa-demo.local`.
2. מלא שם רק סיסמאות דמו, לא אמיתיות.
3. הרץ `npm run qa:create-demo-role-users`.
4. נריץ AUTHED UX/UI QA 3 עם כל התפקידים.

## האם Parent query עדיין תקין?

כן ברמת קוד ובנייה. השדה השגוי `children.pickup_status` כבר לא נבחר בשאילתה.

## האם אפשר לעבור ל-Controlled Pilot Prep?

לא עדיין.

אפשר לעבור ל-AUTHED UX/UI QA 3 רק אחרי שיש דרך בטוחה להתחבר לכל התפקידים.
