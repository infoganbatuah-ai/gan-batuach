# Mobile camera onboarding — PUSH 17C

## PUSH 17D INTERNAL CLOSURE

READY as an internal Product/API contract; real native-device validation remains external. Mobile starts the same Add Cameras session, can submit only bounded short-lived capability receipts through the authenticated native bridge, and returns to the canonical server assessment. The browser never pretends to scan the LAN. Zero-install remains phone-independent after setup.

When a local bridge is genuinely required, the same session can share a ten-minute one-time pairing file and authenticated Mac/Windows package link. The Connector heartbeat resumes that session and discovery results return to the phone/web wizard. No-computer is expected and proceeds to last-resort appliance evaluation only after lower-friction paths are exhausted.

## MOBILE-FIRST PRINCIPLE

A computer is not a Digital Observer entry requirement. Start on phone/web → Add Cameras → identify/discover → canonical assessment. Prefer secure persistent zero-install. Never ask about a computer before evaluating currently supported persistent paths.

## LOCAL NETWORK DISCOVERY

`mobile-camera-setup.ts` defines bounded temporary discovery receipts (32 safe candidate references, ten-minute lifetime). Real native scanning integration is unavailable in this checkout. The browser does not pretend it can enumerate a LAN. An existing authorized Connector can provide discovery; otherwise manufacturer/app identification remains available.

## PERMISSIONS

Use “נדרשת גישה זמנית לרשת המקומית כדי למצוא את המצלמות שלך.” Denied permission offers settings/retry or manual manufacturer identification, not protocol configuration. OS support must be checked before requesting permission; do not show a working native button without the native capability.

## VENDOR AUTHORIZATION

Prefer vendor OAuth/account consent over cloud-password collection. Nest Device Access is documented but its executable DO integration is absent. Tapo partner API documentation does not by itself prove C211 persistent third-party video support. No vendor account is linked during this push.

## ZERO-INSTALL

Requires authorized secure recoverable transport, no persistent DO customer-side software/hardware, no unsafe inbound exposure, and persistence after setup phone/computer departure. Ranking proof is server-owned, never accepted from client or learned scores. No real zero-install reference is currently available.

## TEMPORARY PROVISIONING

Discovery/permission receipts do not grant permanent monitoring authorization. Mobile provisioning must establish a separately verified persistent path; phone going offline must not stop monitoring. Contract tests cover scope, expiry, permission denial and false persistence.

## CONNECTOR REQUIRED

Only after assessment finds no usable persistent path and verifies current local adapter coverage. Explain “current supported path”, not “this camera is technically incapable of any cloud connection”. Ask about a suitable always-on computer only now.

## COMPUTER HANDOFF

17B's short-lived document/original-account confirmation is preserved. 17C re-evaluates canonical strategy server-side before issuing any new install intent, so directly calling that endpoint cannot force installation when another route is preferred. Public installer distribution and full graphical journey remain incomplete; no technical workaround is certified as consumer success.

## SAME SESSION CONTINUITY

Safe intent UUID restores installation stage in the same Site. Fresh scoped heartbeat triggers same-wizard camera results. Full universal session persistence across identification/selection/activation is not complete. Expired restored intents may require re-identification before a new request; no silent cross-account binding.

## NO COMPUTER

Not an account/product failure. Unknown system continues identification. Integration missing remains integration missing. For the verified local path, absence of a suitable host permits local-appliance evaluation, never an instruction to buy a computer.

## GATEWAY ESCALATION

Last-resort candidate, with reason NO_SUITABLE_HOST for current local coverage. Do not call that universal technical necessity. Enterprise policy remains explicit future scope; no hardware order or migration is performed.

## SECURITY

No passwords, private addresses, raw QR contents or topology in learning receipts. Site/session binding, strict schemas, short lifetime, original-actor approval and fresh authorization remain mandatory. Telemetry is advisory and cannot authorize activation or install software.

## REAL VALIDATION STATUS

EXTERNAL VALIDATION GAP for native mobile device/OS. Web tool inventory timed out; no visual E2E was obtained. Mobile-first contracts pass, but full mobile customer flow is NOT READY because installer and executable continuation gaps also remain internally. Do not attribute all gaps to unavailable phones.
