/** Read-only proof: one real Tapo sample traverses the isolated durable queue. */
import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { execFileSync } from "node:child_process";
import { mkdtempSync, readFileSync, rmSync } from "node:fs";
import { homedir, tmpdir } from "node:os";
import { join } from "node:path";
import { createAiJob } from "../../services/video-gateway/ai-job-contract.mjs";
import { createDurableAiJobQueue } from "../../services/video-gateway/durable-ai-job-queue.mjs";
import { createPortableInferenceWorker } from "../../services/video-gateway/portable-inference-worker.mjs";
import { JournalTracker } from "../../services/video-gateway/journal-tracker.mjs";

const root=mkdtempSync(join(tmpdir(),"observer-real-ai-job-"));const secretRoot=process.env.OBSERVER_CONNECTOR_SECRET_DIR||join(homedir(),"Library","Application Support","Digital Observer","Tapo Connector","secrets");
try{
  const gatewaySecret=readFileSync(join(secretRoot,"gateway_signing_secret"),"utf8").trim();const streamId=readFileSync(join(secretRoot,"connector_gateway_stream_id"),"utf8").trim();const sourceId=readFileSync(join(secretRoot,"connector_camera_source_id"),"utf8").trim();const deviceId=readFileSync(join(secretRoot,"device_gateway_id"),"utf8").trim();const siteId=readFileSync(join(secretRoot,"device_observer_site_id"),"utf8").trim();
  const dvrService="com.ganbatuach.video-gateway.runtime";const profile=JSON.parse(execFileSync("/usr/bin/security",["find-generic-password","-s",dvrService,"-a","dvr_profile_json","-w"],{encoding:"utf8",stdio:["ignore","pipe","ignore"]}));const host=new URL(profile.endpoint.includes("://")?profile.endpoint:`http://${profile.endpoint}`).hostname;const namespace=String(profile.metadata?.stream_namespace||"").trim().replace(/[^a-zA-Z0-9._:-]/g,"").slice(0,80);const dvrStreams=[1,2,3,4,5,6,7,8,10,11].map(channel=>`dvr_${createHash("sha256").update([profile.connection_type||"dvr",host,channel,namespace].join(":")).digest("hex").slice(0,18)}_${channel}`);
  const healthBefore=await Promise.all([fetch("http://127.0.0.1:18082/health",{signal:AbortSignal.timeout(2_000)}).then(r=>r.json()),fetch("http://127.0.0.1:18083/health",{signal:AbortSignal.timeout(2_000)}).then(r=>r.json())]);
  const workerCapability=Symbol("real-home-isolated-qa-worker");const queue=createDurableAiJobQueue({databasePath:join(root,"ai.sqlite"),workerAuthorizer:worker=>worker.identity?.local_capability===workerCapability});const now=Date.now();
  for(const dvrStream of dvrStreams)queue.enqueue(createAiJob({tenant_id:siteId,site_id:siteId,source_id:dvrStream,observation_timestamp:new Date(now).toISOString(),priority:"NORMAL",purpose:"REALTIME_DETECTION",requested_capability:"OBJECT_DETECTION",model_class:"GENERAL_OBJECT_DETECTION",input_ref:{kind:"GATEWAY_SOURCE_SAMPLE",reference:`stream:${dvrStream}`,locality:"MANAGED_COMPONENT_ONLY"},expires_at:new Date(now+90_000).toISOString(),scheduler_reason:"READ_ONLY_REAL_HOME_EMISSION_PROOF",scheduler_version:"observer-adaptive-sampling-v1"}));
  const queued=queue.enqueue(createAiJob({tenant_id:siteId,site_id:siteId,source_id:sourceId,observation_timestamp:new Date(now).toISOString(),priority:"CRITICAL",purpose:"REALTIME_DETECTION",requested_capability:"OBJECT_DETECTION",model_class:"GENERAL_OBJECT_DETECTION",input_ref:{kind:"GATEWAY_SOURCE_SAMPLE",reference:`stream:${streamId}`,locality:"MANAGED_COMPONENT_ONLY"},expires_at:new Date(now+90_000).toISOString(),scheduler_reason:"READ_ONLY_REAL_CAMERA_PROOF",scheduler_version:"observer-adaptive-sampling-v1"}));
  const worker=createPortableInferenceWorker({workerId:"real-tapo-isolated-qa",environment:"EDGE_LOCAL",identity:{authenticated:true,revoked:false,device_id:deviceId,tenant_ids:[siteId],site_ids:[siteId],local_capability:workerCapability},capabilities:["OBJECT_DETECTION"],infer:async job=>{const response=await fetch(`http://127.0.0.1:18083/camera/${encodeURIComponent(job.input_ref.reference.slice(7))}/detections`,{headers:{"x-video-gateway-secret":gatewaySecret},signal:AbortSignal.timeout(75_000)});const body=await response.json();if(!response.ok||body.insight?.object_detection?.status!=="sampled")throw new Error("real_detector_unavailable");return{detections:body.insight.object_detection.detections,source_anchor:body.insight.source_anchor,observation_timestamp:body.insight.sampled_at,model_provenance:body.insight.object_detection.model_provenance};}});
  const processed=await worker.processOne(queue);assert.equal(processed.status,"COMPLETED");const result=queue.result(queued.job.job_id,{consume:true});assert(result);assert.equal(result.source_id,sourceId);assert.equal(result.worker_environment,"EDGE_LOCAL");
  const anchor=result.source_anchor;const tracker=new JournalTracker({personConfirmations:99});const events=tracker.observe({camera_id:sourceId,stream_id:streamId,monitoring_enabled:true,source_mode:"real_camera",zone_type:"INDOOR",allowed_event_types:["person_detected"],supported_event_types:["person_detected"],verified_event_types:[],critical_event_types:[]},result.detections,result.observation_timestamp,anchor,{model:result.model,runtime:result.runtime,execution_provider:"cpu",expected_sha256:/^[a-f0-9]{64}$/i.test(result.model_version)?result.model_version:undefined});
  const healthAfter=await Promise.all([fetch("http://127.0.0.1:18082/health",{signal:AbortSignal.timeout(2_000)}).then(r=>r.json()),fetch("http://127.0.0.1:18083/health",{signal:AbortSignal.timeout(2_000)}).then(r=>r.json())]);const snapshot=queue.snapshot();queue.close();
  console.log(JSON.stringify({status:"PASS",mode:"READ_ONLY_REAL_CAMERA_SAMPLE",physical_device:"Tapo C211",path:"Software Connector → durable AI job → eligible ONNX worker → inference result → canonical JournalTracker",real_home_job_emission:{dvr_physical_sources:dvrStreams.length,tapo_sources:1,total:11,empty_dvr_slots:6,empty_slot_jobs:0},job_id:result.job_id,detections:result.detections.length,canonical_events_naturally_emitted:events.length,event_fabricated:false,queue_wait_ms:result.queue_wait_ms,inference_ms:result.inference_ms,total_latency_ms:result.queue_wait_ms+result.inference_ms,latency_sample_count:1,model:result.model,runtime:result.runtime,result_consumed_once:true,isolated_pending_dvr_jobs_discarded_with_qa_database:snapshot.queue_depth,dvr_progressing_before:healthBefore[0].mediaHeartbeat?.progressingRelays??null,dvr_progressing_after:healthAfter[0].mediaHeartbeat?.progressingRelays??null,dvr_stalled_after:healthAfter[0].mediaHeartbeat?.stalledRelays??null,tapo_progressing_before:healthBefore[1].mediaHeartbeat?.progressingRelays??null,tapo_progressing_after:healthAfter[1].mediaHeartbeat?.progressingRelays??null,tapo_stalled_after:healthAfter[1].mediaHeartbeat?.stalledRelays??null,source_configuration_changed:false,raw_media_logged:false},null,2));
}finally{rmSync(root,{recursive:true,force:true});}
