# תצפיתן דיגיטלי - דוח חוויית מוצר AI-First

תאריך: 21.8.2026

## תוצאה

חוויית התצפיתן הועברה ממסכי מוכנות וטפסים בלבד אל מרכז עבודה שמציג מידע מאוחסן אמיתי או סינתטי, מסביר מה ידוע ומה לא ידוע, ומאפשר למשתמש לשאול, לתת משוב ולהדגיש מה חשוב לבדוק. שירותים חיים רגישים נשארו כבויים.

## מה הוטמע

### מרכז תצפיתן ושיחה

- `app/digital-observer/dashboard/page.tsx` מציג מרכז מצב ביתי ועסקי, כניסה/יציאה, רכב וחניה, אנשים לא מוכרים וחריגות על בסיס אירועים קיימים בלבד.
- `app/digital-observer/rules/page.tsx` מציג את "התצפיתן שלי", מצב המנוע, מצלמות, מסלול תצפית חיה, מחזור שיפור מדיד ושיחה בשפה טבעית.
- `components/digital-observer/observer-intelligence-experience.tsx` מספק שיחה, שאלות מהירות וביקורת מועמדי אדם שכיח.
- `app/api/digital-observer/conversation/route.ts` עונה מתוך האירועים, המצלמות וקווי הבסיס של האתר בלבד. הוא אינו ממציא פעילות ואינו מפעיל חירום.

### אנשים שכיחים ומוכרים

- `app/digital-observer/people/page.tsx` מציג מועמדים שהתקבלו ממנוע הווידאו, ולא תמונות דמה.
- `supabase/migrations/20260821000400_digital_observer_identity_candidate_review.sql` מוסיף תור מועמדים מופרד לפי אתר.
- תמונת מועמד נמסרת רק דרך `app/api/digital-observer/identity-candidates/[id]/preview/route.ts` כקישור חתום קצר לאחר בדיקת הרשאה.
- הגדרת אדם מוכר דורשת שם והסכמה מפורשת ונשארת במצב `readiness`; אין הפעלה אוטומטית של ביומטריה.
- אתר המטפל בילדים או פועל במצב `skeleton_only` נחסם מתהליך זיהוי פנים.

### חיבור מצלמה

- `lib/domain/digital-observer/camera-connection-methods.ts` מגדיר מסלולי QR, אפליקציית יצרן, איתור ברשת, NVR/DVR, חיבור ידני ו-Gateway.
- `components/digital-observer/observer-action-forms.tsx` מציג אשף חיבור ידידותי ומסביר מה נדרש בכל מסלול.
- סריקת QR מעובדת בדפדפן ואינה שומרת תמונה או payload גולמי.
- `app/api/digital-observer/cameras/route.ts` שומר רק metadata בטוח. סיסמת מצלמה, RTSP וסוד Gateway אינם נשמרים בצד הלקוח.

### התאמה לבית ולענפים עסקיים

- `lib/domain/digital-observer/site-templates.ts` מגדיר תבניות לבית, קיוסק, חנות, משרד, מחסן, מרפאה, מסעדה, מסגרת ילדים ועסק מותאם.
- אשף ההקמה מאפשר לבחור תבנית ומטעין יעדי ניטור התחלתיים שניתנים לשינוי.
- תבנית היא policy reusable: היא אינה מפעילה ספק או שירות, וכל תבנית מחייבת ביקורת אנושית וחוסמת פעולת חירום אוטומטית.
- חשד לגניבה, שוד, עימות או אלימות מוצג כהערכה עם רמת ביטחון והחלטה אנושית, לא כעובדה.

### מציאות נתונים ופעולות

- נוספו סוגי אירועים: כניסה/יציאה, רכב, חניה, אדם לא מוכר, חשד לפגיעה ברכב, מצוקה, חדר/אזור, דלת פתוחה ומצלמה מכוסה.
- כפתורים שאין מאחוריהם שירות חי הוחלפו במצב מוכנות מוסבר.
- אין תווית `LIVE`, תשלום מוצלח, הודעה שנשלחה או זיהוי ביומטרי ללא מקור וספק פעילים.
- זמן למידה מוצג כחלון איסוף, לא כציון אמינות.

### מובייל, טאבלט ודסקטופ

- הניווט במובייל מרכז את "התצפיתן שלי" כפעולה הראשית.
- חסימת pinch zoom הוסרה מ-viewport של המוצר ומה-layout הראשי.
- נוספו `app/digital-observer/loading.tsx` ו-`app/digital-observer/error.tsx` למסכי טעינה והתאוששות אמיתיים.
- מסכי הדשבורד, התצפיתן, אנשים והוספת מצלמה נבדקו בחשבון ביתי ועסקי.
- נשמרו 30 צילומי production תחת `qa-evidence/digital-observer-ai-experience-1`: דשבורד, התצפיתן שלי, אנשים מוכרים, הוספת מצלמה והקמה עסקית בחשבון ביתי ועסקי.
- הדוח `qa-evidence/digital-observer-ai-experience-1/REPORT.md` עבר ללא גלילה אופקית ובניווט מובייל תקין ב-390x844, טאבלט 820x1180 ודסקטופ 1440x900.
- בדיקת הטאבלט חשפה שדות radio נסתרים שקיבלו בטעות רוחב מסך מלא מחוק גלובלי. `app/styles/digital-observer-product.css` תוקן והסבב החוזר עבר 30/30.
- הראיות צולמו מ-build production. בועת `N` שנראתה בעבר הייתה Next.js dev indicator ואינה מופיעה בראיות הסופיות.
- קבצי `page 2.tsx` הישנים אינם מסלולים פעילים; רשימת הנתיבים של build production מכילה רק את מסלולי `page.tsx` התקניים.

## Supabase ואבטחה

- המיגרציה החדשה הוחלה בפרויקט `gan-batuah` והחזירה `Success`.
- `npm run qa:digital-observer-product` עבר 44 מתוך 44 בדיקות.
- נבדקה הפרדה בין חשבון ביתי לעסקי עבור אתרים, מצלמות, אירועים, אנשים, מועמדים, הקלטות ומסירות.
- שדות `cluster_reference` ונתיבי מדיה פרטיים אינם ניתנים לקריאה מתפקיד דפדפן.
- לא נוסף service role ללקוח ולא נחלש RLS.

## מה עדיין אינו חי

- Camera Gateway ו-stream מורשה.
- AI inference ו-clustering במצב Shadow.
- זיהוי פנים חי או ביומטריה פעילה.
- Push, SMS, WhatsApp ושיחות.
- תשלום חי.
- חיוג או דיווח למוקד חירום.

המסכים מציגים את התחומים האלה כמוכנות בלבד. אין לראות בדוח זה הוכחה ליכולת 24/7 או לאמינות גבוהה מתצפיתן אנושי.

## החלטה נדרשת

השלב הבא הוא בחירת סביבת Gateway ומקור בדיקה מורשה. ההמלצה נמצאת ב-`DIGITAL_OBSERVER_GATEWAY_PROVIDER_DECISION_HE.md`: MediaMTX כליבה ו-go2rtc אופציונלי ב-Edge.

## שערי איכות סופיים

- `npm run typecheck`: עבר.
- `npm run build`: עבר, 467 מסכים נבנו.
- `npm run qa:digital-observer-product`: עבר 44/44 מול חשבונות הדמו ו-Supabase.
- QA ויזואלי production: עבר 30/30 ללא overflow.
- `npm run mobile:sync`: עבר עבור Android, iOS ו-Web; בדיקת מכשיר אמיתי עדיין נדרשת לפני הפצת אפליקציה.
- `git diff --check`: עבר לפני עדכון הדוח ויורץ שוב כשער אחרון.
- לא בוצע push ולא הופעל שירות חי רגיש.
