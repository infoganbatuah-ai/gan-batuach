# דוח קבלה עצמאי - תצפיתן דיגיטלי

תאריך בדיקה: 21.8.2026  
סוג בדיקה: קריאה ובדיקה בלבד, ללא שינוי קוד, ללא יצירת חשבונות, ללא הפעלת ספקים, מצלמות, ביומטריה, התראות או פעולות חירום.

## החלטת קבלה

**סטטוס: קבלה חלקית / לא מוכן לשחרור מלא.**

המוצר התקדם משמעותית מחוויית "טפסים ודסקטופ" לחוויית אפליקציה מוביילית בעברית/RTL: יש ניווט תחתון, מסכי בית ועסק נפרדים, שיחה עם התצפיתן, נתוני אירועים, אנשים, מצלמות ואשף חיבור. עם זאת, אין בסיס מספיק לקבלת מוצר חי מלא משום שהבדיקות האוטומטיות והשרת המקומי לא רצו בעץ הנוכחי בגלל חסרון `node_modules`, קיימות בעיות נגישות, וצילומי הייחוס עצמם אינם נקיים לגמרי.

## ראיות שנבדקו בפועל

- נצפו ידנית צילומי מובייל קיימים תחת `qa-evidence/digital-observer-ai-experience-1`:
  - `home-dashboard-mobile.png`
  - `business-dashboard-mobile.png`
  - `home-observer-mobile.png`
  - `business-observer-mobile.png`
  - `home-people-mobile.png`
  - `business-people-mobile.png`
  - `home-camera-add-mobile.png`
  - `business-camera-add-mobile.png`
- אומת זמן יצירת הראיות: 21.8.2026 בשעה 04:51:46 לפי metadata מקומי.
- נקרא דוח הראיות `qa-evidence/digital-observer-ai-experience-1/REPORT.md`, שמצהיר על 24 צילומים ועל PASS לפריסה, main, overflow וניווט מובייל.
- בוצעה קריאת קוד ממוקדת למסכים ול-API:
  - `app/digital-observer/dashboard/page.tsx`
  - `components/digital-observer/observer-app-shell.tsx`
  - `components/digital-observer/observer-intelligence-experience.tsx`
  - `app/styles/digital-observer-product.css`
  - `lib/domain/digital-observer/access.ts`
  - `lib/domain/digital-observer/runtime.ts`
  - `app/api/digital-observer/conversation/route.ts`
  - `app/api/digital-observer/cameras/route.ts`
  - `app/api/digital-observer/identity-candidates/route.ts`
  - `app/api/digital-observer/identity-candidates/[id]/preview/route.ts`
- ניסיון הרצה בפועל:
  - `npm run qa:digital-observer-product` נכשל כי `@supabase/supabase-js` לא מותקן.
  - `npm run typecheck` נכשל כי `tsc` לא מותקן.

## מה עובר

1. **מובייל כערוץ ראשי** - בצילומי 390x844 המסכים אינם נראים כמו דסקטופ מכווץ: יש top bar קומפקטי, ניווט תחתון קבוע, פעולת תצפיתן מרכזית, כרטיסי KPI ומסכי אשף. זה עובר ברמת עיצוב נצפה.
2. **עברית ו-RTL** - ה-root מוגדר `lang="he"` ו-`dir="rtl"`, המעטפת הפנימית מגדירה `dir="rtl"`, וה-manifest של התצפיתן כולל `lang: "he"` ו-`dir: "rtl"`.
3. **הפרדה בין בית לעסק** - המסכים והניווט משתנים לפי mode: בבית מופיעים "מסך הבית", "מה קורה בבית" ו"אבני דרך ביתיות"; בעסק מופיעים "סקירת העסק", אתרים, מצלמות, מנוי וחיוב.
4. **חוויה דינמית ולא סטטית בלבד** - שיחת התצפיתן קוראת אירועים, מצלמות ודפוסי שגרה שמורים; כשמשתמש כותב "שים לב/תתריע" היא שומרת בקשת ניטור במצב מוכנות. זה חי מבחינת interaction ונתונים, גם אם לא מפעיל AI/וידאו חי.
5. **אמת מוצרית לגבי שירותים כבויים** - הקוד והמסכים נמנעים מתווית LIVE מזויפת ומחזירים מסרים כגון Gateway/AI/התראות חיצוניות רק לאחר ספקים מאושרים.
6. **הרשאות ופרטיות בין אתרים** - קריאות API עוברות דרך `getDigitalObserverApiUser` ו-`getObserverSiteAccess`; גני ילדים מוחרגים, גישה תלויה בבעלות/חברות, ותמונות מועמדים נחתמות רק אחרי בדיקת הרשאה.
7. **ביומטריה וילדים** - מסלול מועמדי זהות חוסם תמונות/זיהוי פנים כאשר `vision_privacy_mode === "skeleton_only"` או `business_handles_children` פעיל, ודורש הסכמה מפורשת לשמירת אדם מוכר.
8. **מצבי טעינה ושגיאה נקודתיים** - בטפסים ובשיחה קיימים מצבי busy, הודעות שגיאה עם `role="alert"` וחיווי הצלחה/סטטוס במספר נקודות.

## מה לא עובר

1. **אי אפשר לאשר build/TypeScript/QA אוטומטי** - אין `node_modules`, ולכן `npm run qa:digital-observer-product` ו-`npm run typecheck` נכשלו לפני בדיקת המוצר. זה חוסם קבלה הנדסית מלאה.
2. **נגישות מובייל נפגעת** - גם root layout וגם layout התצפיתן מגדירים `maximumScale: 1`, ולכן משתמשי מובייל לא יכולים להגדיל את המסך. זה כשל WCAG/נגישות ברור.
3. **צילומי הייחוס לא נקיים לגמרי** - בכל צילומי המובייל שנצפו מופיעה בועה שחורה עם `N` בפינה התחתונה-שמאלית, מעל הניווט התחתון. זה נראה כמו שכבת dev/תוסף שנכנסה לראיות, ולכן אין לקבל את הצילומים כהוכחת מוצר נקייה.
4. **אין בדיקה חיה חוזרת שלי בדפדפן** - בגלל חסרון חבילות מקומי לא הופעל שרת ולא נוצרו צילומי מסך חדשים בסשן הזה. הממצאים הוויזואליים מבוססים על צפייה בפועל בראיות קיימות, לא על capture חדש.
5. **אין קובצי `loading.tsx` / `error.tsx` מקומיים תחת `app/digital-observer`** - קיימים מצבי טעינה/שגיאה ברכיבים, אך אין גבול שגיאה/טעינה ייעודי למסלולי התצפיתן. זה סיכון לחוויית משתמש בעת תקלות מסלול או טעינת server components.
6. **יש קבצים כפולים `page 2.tsx` במסלולי התצפיתן** - הם לא מסלולי Next פעילים, אבל הם מגדילים סיכון בלבול, תחזוקה וייבוא לא מכוון.
7. **הצהרת PASS קודמת רחבה מדי** - `qa-evidence/digital-observer-ai-experience-1/REPORT.md` מסמן Final result PASS, אך גם מציין שאינו מוכיח מצלמה חיה, ביומטריה, חיוב, התראות או חירום. לכן PASS זה תקף לפריסה ויזואלית בלבד, לא למוצר חי מלא.

## סדר תיקון מומלץ

1. **להחזיר סביבת בדיקה ניתנת להרצה** - להתקין dependencies בסביבת QA, להריץ `npm run typecheck`, `npm run build` ו-`npm run qa:digital-observer-product`, ואז לשמור פלטים חדשים.
2. **לתקן נגישות זום** - להסיר `maximumScale: 1` מה-root ומה-layout של התצפיתן, ולאמת זום מובייל.
3. **לצלם ראיות נקיות מחדש** - להריץ capture ללא שכבת `N`, על 390x844, 430x932, 820x1180 ו-1440x900, לבית ולעסק.
4. **להוסיף `loading.tsx` ו-`error.tsx` ייעודיים לתצפיתן** - עם עברית, RTL, כפתור חזרה בטוח והודעת retry.
5. **לנקות קבצי `page 2.tsx`** - לאחר וידוא שאינם בשימוש, להעביר לארכיון או להסיר כדי לצמצם סיכון תחזוקה.
6. **להריץ בדיקת הרשאות חיה לאחר dependencies** - להוכיח RLS בין home/business, viewer/operator/admin, תמונות מועמד, יצירת watch request, והגדרות התראות.
7. **להגדיר שער קבלה נפרד ל"חי"** - Gateway מצלמות, AI Shadow, billing, push/SMS/WhatsApp וחירום צריכים להישאר FAIL/Not Tested עד הפעלה יזומה ומורשית.

## סיכום

התצפיתן הדיגיטלי כבר נראה ומתנהג יותר כמו מוצר אפליקטיבי מוביילי ודינמי מאשר דשבורד דסקטופ זול. הקבלה החלקית מוצדקת למסכי UX/RTL/מובייל ברמת ראיות קיימות וקריאת קוד. הקבלה המלאה חסומה עד בדיקות הרצה נקיות, תיקון נגישות הזום, וצילומי מסך חדשים ללא שכבות dev.
