# DEMO AUTH CREDENTIALS 1 - סיכום לדניאל

## מה מוכן?

- יש קובץ דוגמה בטוח: `.env.qa-demo.example`.
- יש סקריפט בטוח ליצירת משתמשי QA חסרים: `npm run qa:create-demo-role-users`.
- יציאה בין תפקידים דרך `/api/auth/logout` מוכנה בקוד.
- בדיקות typecheck/build עברו.

## מה חסר?

יש קובץ מקומי:

`.env.qa-demo.local`

עכשיו קיימות בו סיסמאות לפי שמות משתנים לכל תפקידי הבדיקה המבוקשים, בלי להדפיס את הערכים ובלי להכניס אותם לקוד.

## אילו משתמשים עדיין צריכים בדיקה/יצירה?

- הורה לא משויך: צריך ליצור/לאמת ב-Supabase.
- צוות לא משויך: צריך ליצור/לאמת ב-Supabase.
- מפקחת לא משויכת: צריך ליצור/לאמת ב-Supabase.
- Digital Observer: צריך ליצור/לאמת כמשתמש אפליקציה ב-Supabase Auth, וצריך גם אתר תצפית דמו.

## האם קובץ הסיסמאות מולא?

כן. קובץ הסיסמאות המקומי מולא לכל התפקידים לפי שמות משתנים בלבד. הסיסמאות לא הודפסו ולא נשמרו בקבצים שמיועדים ל-git.

## האם בדיקת התחברות עברה?

לא בוצעה בשלב הזה. אפשר להריץ בדיקת התחברות חלקית לתפקידים שכבר קיימים. QA מלא עדיין חסום עד יצירת/אימות המשתמשים החסרים ב-Supabase.

## האם AUTHED UX/UI QA 3 יכול לרוץ עכשיו?

לא בגרסה מלאה.

אפשר להריץ QA חלקי עבור:

- הורה משויך
- מנהלת
- צוות משויך
- מפקחת משויכת
- אדמין

## מה הצעד הבא המדויק?

1. מלא או צור את המשתמשים החסרים:

- `qa.parent.unassigned@demo.ganbatuach.com`
- `qa.staff.unassigned@demo.ganbatuach.com`
- `qa.inspector.unassigned@demo.ganbatuach.com`
- `qa.digital.observer@demo.ganbatuach.com`

2. אם רוצים ש-Codex ייצור אותם דרך הסקריפט, צריך להוסיף ל-`.env.qa-demo.local` גם `NEXT_PUBLIC_SUPABASE_URL` ו-`SUPABASE_SERVICE_ROLE_KEY`. הערכים האלה לא יופיעו בדוחות ולא יודפסו.

3. אחרי זה אפשר לבקש מ-Codex להריץ AUTHED UX/UI QA 3 מלא.

לא לעבור להכנת פיילוט עד שכל הדשבורדים המחוברים נבדקים באמת.
