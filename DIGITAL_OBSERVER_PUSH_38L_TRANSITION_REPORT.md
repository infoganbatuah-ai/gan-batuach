# PUSH 38L — Connector legacy-to-managed transition

Date: 2026-09-13. **READY FOR A SEPARATELY AUTHORIZED LIVE QA/PILOT TRANSITION**, not commercial distribution. PUSH 38 remains NOT DONE. No live Connector or Gateway runtime, service, trust root, identity, configuration, queue, source or Site was modified. Pre-soak and v8 remain NOT STARTED; PR #28 remains draft/unmerged.

## Exact legacy and forensic boundary

The captured exact live Connector archive is `qa-legacy-connector-ee82c20a77ac`, SHA-256 `ee82c20a77acb7fd8caf692682569ad53581e9c057b11724845c6d947c5a982a`. Its 250 inventoried members match the installed runtime, but the original app seal fails strict macOS verification because files were added/modified after signing. It is labeled **NON-CANONICAL SIGNATURE / LEGACY_RECOVERY_ONLY**. A QA Ed25519 manifest authorizes custody/integrity of that capture; it does not repair the Apple code seal or make the archive a normal managed release.

The original and separately re-signed archives each contain 250 files. Exactly 248 non-signing files are byte-identical. The only changed members are the app's `Contents/_CodeSignature/CodeResources` seal and `Contents/MacOS/DigitalObserver` Mach-O executable; isolated copies of both executables become **byte-identical after removing their code signatures**. The normalized executable/payload inventory digest is `81cc985adc0e8e8cc4af86ad13f47ec6998ed82fbc080b0e5fc63e53eecc4986`. The current legacy runner content matches Git blob `29e575540362327b3299decbe80a5b034f0d3b9d` at `dfbd604f`; the supporting cloud/auth source blobs match `d924ea9d`. This accounts for the post-sign source-code changes seen in strict verification; historical backup files under the live app are outside the captured release boundary. No unexplained transition-payload delta was found. The installed legacy package does **not** have a single proven Git build commit, and none is invented.

## New signed transition artifact

The distinct QA transition release is `qa-connector-legacy-transition-v2-6e7988808b05`, version `0.1.0-legacy`, artifact SHA-256 `6e7988808b05956d58416a6ce60638f52b19aa732918ac0e1cdafcc5fc9f130a`. Its `build_sha` is this artifact digest, **not a source commit**. It derives from the exact captured legacy payload through the isolated 38G QA re-signing step; the new Ed25519 manifest is signed by `qa-p38f-ed25519-20260913`. A separate signed `observer-connector-legacy-transition-v2` derivation record binds the legacy hash, transition hash, normalized payload digest, resource-inventory digest `1ce102e6af09280858c548c78fcfc16443a19271a55e9cf83abb2057e098a9cf`, verified mixed-Git-blob lineage, external configuration expectation, runtime dependencies, the two intentional signing transformations, profile/architecture, purpose and device ID. A live-device-bound QA record was prepared in controlled storage without writing to the installed Connector; no device identifier or private signing key is committed. The earlier v1 QA record/release are historical evidence, not the proposed live transition. The QA-only archive and record remain outside Git. Both the final app and an extracted final package pass strict macOS verification. This is ad-hoc QA signing, **not** Developer ID or notarization.

## One-time state and rollback rule

The new one-time coordinator is Connector-only, INTERNAL-channel, device-bound and requires both signed archive manifests plus the signed derivation record. It compares the exact legacy archive to the installed bytes, then retains that archive outside normal OTA slots as `LEGACY_RECOVERY_ONLY`. It installs only the strict-valid transition archive into a managed slot. After service-manager handoff, health and continuity must pass before a single signed transition pointer becomes CURRENT and KNOWN_GOOD. The legacy recovery journal is then `RETIRED`; normal OTA can never select it. Normal update/rollback code additionally rejects the exact legacy release ID or hash and rejects rollback slots outside the managed slot root. Gateway retains its ordinary signed-baseline path.

If first migration fails **before promotion**, the coordinator restores the original legacy LaunchAgent, rechecks the exact archived legacy bytes and identity/binding fingerprints, records `ACTION_REQUIRED`, and does not retry automatically. A second call cannot create a legacy→transition→legacy loop. Interrupted pre-promotion state is recoverable through this explicit one-time path; a healthy signed transition already promoted is reconciled as managed, not reverted to invalid-seal legacy.

## Isolated and live-read-only evidence

Actual isolated macOS LaunchAgent QA passed: exact invalid-seal legacy start under an explicitly scoped QA-only startup exception; one-authoritative-process handoff to the strict-valid transition; signed transition promotion; installed OTA agent discovering the final signed PUSH 38I remediation release without manual updater invocation; health PASS; a signed controlled crash-loop update automatically returning to the **signed transition hash** `6e7988…`; later crash-loop rollback returning to the then-promoted signed remediation. A separate isolated forced first-transition health failure restored the exact legacy archive hash `ee82c2…` under LaunchAgent and entered ACTION_REQUIRED with no loop. Identity/config/queue/source-map fixtures were unchanged in both drills. The QA-only startup exception is restricted to the exact captured legacy release in isolated QA; normal managed Connector installs retain strict signature checks.

The final v2 release/derivation was rechecked through the full installed-agent/LaunchAgent lifecycle, including exact signed-target rollback and failed-release quarantine. The standalone policy fixture rejects a changed signed record and wrong device, blocks legacy as normal OTA, rejects a repeat migration, and preserves external identity/config/queue fixtures. Existing release-trust, OTA, profile/architecture/tamper/downgrade, strict Connector post-sign-mutation, typecheck and lint-baseline gates pass. These are isolated QA proofs, not a live migration or 60-minute camera stability claim.

The final **zero-write** live planner matched the exact 250-member Connector legacy capture, verified the live-device-bound signed record, verified the transition archive/strict signature, found external identity/config/data paths and no supervisor/slot-layout conflicts. The Gateway's approved baseline still matched in the same planner. This proves compatibility of the *plan*, not live migration or camera reliability. A later live operator must repeat hash/health checks, install a protected QA trust root through the authorized local-admin step, then execute the first transition with a staged recovery path. No live trust root or OTA agent was installed here.

## Future live sequence, not executed

1. Recheck exact Connector hash, health and identity/Site/source inventory; stop on drift.
2. Verify the device-bound derivation record, both signed archive digests, transition app strict signature and external persistent-state paths.
3. Register the exact invalid-seal archive as LEGACY_RECOVERY_ONLY, **never** as managed KNOWN_GOOD.
4. Install authorized trust root/registry and management metadata, stage the strict-valid transition, perform one service-manager handoff, and verify Tapo/identity/source continuity.
5. Promote the signed transition to CURRENT and KNOWN_GOOD only after health and playback/AI gates pass; retire legacy from normal execution.
6. Start the installed OTA agent and expose the signed PUSH 38 remediation release. Any first-migration failure before promotion restores exact legacy and stops ACTION_REQUIRED; later normal rollback uses only signed managed releases.

Production release-signing custody, Apple Developer ID distribution signing and notarization remain external gaps. No 60-minute pre-soak or v8 evidence is claimed. The deferred billing-role RLS finding is unchanged and outside this PUSH 38L scope.

**Readiness decision:** zero unresolved HIGH/CRITICAL *internal transition-policy* blockers after the isolated v2 lifecycle and read-only live compatibility checks. This does not close the broader PUSH 38 reliability qualification. A live transition still requires the separately authorized, staged pilot operation and immediate pre-write baseline/health recheck.
