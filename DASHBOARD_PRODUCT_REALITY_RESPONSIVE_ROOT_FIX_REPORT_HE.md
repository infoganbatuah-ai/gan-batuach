# תיקון שורש: דשבורדים רספונסיביים ומוצר אפליקטיבי

תאריך: 14.08.2026

## סיכום מנהלים

בוצע תיקון שורש למעטפות הדשבורד של הורה, מנהלת, צוות, מפקח, אדמין ו-Digital Observer. הבעיה שבה המסך הסתדר רק לאחר שינוי ידני של רוחב החלון תוקנה. בבדיקה מחוברת אמיתית לחשבונות הסינתטיים, כל שמונת מצבי המשתמש עברו טעינה ראשונה במובייל, טאבלט ודסקטופ ללא גלישה לרוחב וללא כפתורים חתוכים.

המסכים אינם עוד שכפול קבוע של תמונת עיצוב: שם המשתמש, תמונת הפרופיל, תאריך ישראל, שיוך לגן, ילדים, צוות, ביקורות ומדדים נקראים מהפרופיל ומהנתונים. כאשר מקור נתונים אינו קיים, מוצג מצב ריק או מוכנות ברור במקום מספר חלופי או הצלחה מדומה.

## גורמי השורש שתוקנו

1. כללי CSS ישנים הגבילו דשבורדים לרוחב טלפון גם בדסקטופ והוסיפו מסגרת מכשיר מדומה.
2. מעטפות נפרדות לכל תפקיד יצרו כותרות וניווט לא עקביים.
3. `filter` במעבר מסכים הפך את סרגל הניווט הקבוע לרכיב שנצמד לתחתית הדף הארוך במקום לתחתית המסך.
4. מצב תצוגת מובייל נשמר ב-`localStorage` והשפיע על פתיחה רגילה בדסקטופ.
5. תמונות, שמות ותאריכים נקראו משדות לא נכונים או מערכי fallback קבועים.
6. שאילתת צוות ביקשה שדות שאינם קיימים (`staff.role`, `staff.preferred_city`) ולכן צוות משויך נראה כאילו אינו משויך.
7. זרימת משפחת הורה השתמשה במסלול שרת שיכול היה לבחור Service Role. המסלול הוסר; הדפים משתמשים כעת בלקוח המשתמש וב-RLS הרגיל.

## תיקוני רספונסיביות

- נוספה שכבת runtime גלובלית ב-`app/styles/dashboard-runtime.css`, שנטענת אחרונה.
- רוחב הדשבורד כעת מבוסס viewport אמיתי, עם רוחב תוכן מרבי של 1440px בדסקטופ רחב.
- עד 1024px מוצג ניווט תחתון; החל מ-1025px מוצג סרגל צד.
- ניווט תחתון כולל safe-area ואינו מכסה תוכן.
- טבלאות, טפסים, מודלים, כרטיסים וכפתורים מקבלים מגבלות רוחב וגלילה עקביות.
- מצב `?view=mobile` נשאר אפשרות בדיקה מפורשת בלבד ואינו נשמר לפתיחה הבאה.

## מעבר ממסך סטטי למוצר חי

- מעטפת משותפת חדשה: `components/role-app-shell.tsx`.
- שם ותמונת משתמש מגיעים מ-`profiles.full_name` ו-`profiles.profile_image_url`.
- קידומת `[DEMO]` מנוקה מהתצוגה, אך סביבת הבדיקה עדיין מסומנת בבירור כהודעת סביבה.
- תאריך כותרת ויום עבודה משתמשים ב-`Asia/Jerusalem` דרך `lib/domain/israel-date.ts`.
- המספרים הקבועים של דשבורד המנהלת הוסרו. חסר מוצג כ-`—` או כמצב ריק, ולא כאפס או נתון מומצא.
- תשלום, מצלמה, AI, WhatsApp ו-SMS מציגים readiness/locked/test בלבד; אין הצלחה חיה מזויפת.
- כרטיס הילד נטען דרך הרשאות ההורה, עם הילד, התמונה, הגן והיומן ששמורים בפועל בנתוני הבדיקה.

## תיקוני נתונים ושאילתות

- הוסר `children.pickup_status` משאילתת ההורה הקודמת.
- לא קיימת עוד בחירת `children.kindergarten_id` לא תקינה בזרימת המשפחה.
- שאילתת צוות תוקנה לשדות הקיימים: `role_title`, `class_group`, `approved_to_work`, `onboarding_status` ו-`profile_photo_url`.
- ספריית הגנים להורה קוראת דרך לקוח המשתמש ולא דרך Service Role.
- רשימת המצלמות, בדיקת ההרשאה ויצירת סשן הצפייה להורה משתמשות רק בסשן המחובר וב-RLS. הוסרו שאילתות ניסוי לשדות/טבלאות שאינם קיימים; כאשר המדיניות או הנתונים אינם מאפשרים צפייה, המערכת נכשלת במצב סגור ומציגה מוכנות/נעילה במקום לעקוף הרשאות.
- הוכנה מיגרציה בטוחה שמפסיקה לולאת RLS ב-Digital Observer בלי להרחיב הרשאות.

## חשבונות בדיקה

נוצרו או עודכנו כל החשבונות הדרושים: הורה משויך ולא משויך, מנהלת, צוות משויך ולא משויך, מפקחת משויכת ולא משויכת, אדמין ו-Digital Observer.

רשימת האימיילים והשיוכים נמצאת ב-`DEMO_TEST_USERS_ASSIGNMENT_REPORT_HE.md`. הסיסמאות נשמרות רק ב-`.env.qa-demo.local`, שמוחרג מ-Git והרשאות הקובץ שלו צומצמו ל-600. הסיסמאות אינן מודפסות בדוחות.

Digital Observer הוא מוצר נפרד לניטור אתרים שאינם גן. משתמש הבדיקה שלו אינו אדמין Gan Batuach ואינו משתמש ניהול Supabase. כתובת הייצור שלו, לאחר פריסה, היא:

`https://gan-batuach.vercel.app/digital-observer/dashboard`

## קבלת מסכים מחוברת

- 8 מצבי משתמש מחוברים.
- 3 גדלי מסך לכל מצב: 390x844, 768x1024, 1440x900.
- 24/24 טעינות ראשונות עברו ללא שינוי ידני של החלון.
- 0 גלישות אופקיות.
- 0 כפתורים או שדות חתוכים.
- 0 שגיאות אפליקציה.
- 0 טענות live מזויפות.
- 72 יעדי פעולה וקישורים נפתחו ללא 404, ללא שגיאת אפליקציה וללא הפניה לא צפויה להתחברות.
- סריקה חוזרת אישרה שאין Service Role בזרימות ההורה, המשפחה או הצפייה במצלמות, ושאין בחירת `children.pickup_status` או `children.kindergarten_id` בזרימת הילד.

הפירוט נמצא ב-`DASHBOARD_AUTHED_RESPONSIVE_ACCEPTANCE_MATRIX.md` והצילומים נמצאים ב-`qa-evidence/dashboard-root-fix/`.

## מה עדיין חסום

ב-Supabase המרוחק קיימת מדיניות ישנה של Digital Observer שגורמת ללולאה בטבלת `observer_site_memberships`. התיקון מוכן בקובץ:

`supabase/migrations/20260814000100_observer_membership_rls_recursion_fix.sql`

המיגרציה עדיין לא הוחלה בפרויקט המרוחק, משום שאין בסביבת העבודה חיבור SQL/CLI מאושר לפרויקט. עד להחלתה, מסך Digital Observer נשאר במצב מוכנות אמיתי ואינו מציג נתונים חיים חלופיים.

בדיקת דפדפן נוספת למסך מצלמות ההורה נחסמה בסיום על ידי מדיניות כתובות ה-URL של כלי הבדיקה. לכן היא אינה נספרת כבדיקה חדשה. תוצאת הקבלה נשענת על בדיקות המסלולים והצילומים שכבר נאספו, ועל אימות קוד חוזר של מסלול המצלמות שנועל גישה דרך RLS ואינו משתמש ב-Service Role.

## המלצה

`DASHBOARDS_READY_FOR_DANIEL_INTERNAL_REVIEW`

הדשבורדים מוכנים כעת לבדיקה פנימית של דניאל בדפדפן. יש להחיל את מיגרציית Digital Observer ולבדוק אותה מחדש לפני אישור מקור הנתונים שלו. לפני בדיקת אפליקציה מקורית יש להריץ `npx cap sync` ולבצע בדיקה במכשיר אמיתי.
