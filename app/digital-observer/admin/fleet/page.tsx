import { Activity, AlertTriangle, HardDrive, RefreshCw, ServerCog, ShieldCheck } from "lucide-react";
import { ObserverAppShell } from "@/components/digital-observer/observer-app-shell";
import { requireDigitalObserverAdmin } from "@/lib/domain/digital-observer/admin-access";
import { loadFleetSnapshot } from "@/lib/domain/digital-observer/fleet-data";

const labels:Record<string,string>={HEALTHY:"תקין",RECOVERING:"מתאושש",OFFLINE:"לא מחובר",AUTH_DEGRADED:"זהות דורשת טיפול",NEEDS_ATTENTION:"דורש טיפול",SOFTWARE_CONNECTOR:"רכיב חיבור תוכנה",PHYSICAL_GATEWAY:"רכיב חיבור פיזי",ENTERPRISE_EDGE:"Enterprise Edge"};
export default async function FleetPage(){const {profile}=await requireDigitalObserverAdmin("/digital-observer/admin/fleet");const data=await loadFleetSnapshot({limit:200});return <ObserverAppShell profile={profile} mode="admin" activeHref="/digital-observer/admin/fleet" title="ניהול רכיבי חיבור" statusLabel="למנהלי מערכת מורשים">
  <div className="do-page-stack"><section className="do-business-summary"><article className="do-metric"><ServerCog/><strong>{data.summary.total}</strong><span>רכיבים מנוהלים</span></article><article className="do-metric"><ShieldCheck/><strong>{data.summary.byHealth.HEALTHY}</strong><span>תקינים</span></article><article className="do-metric alert"><AlertTriangle/><strong>{data.summary.byHealth.OFFLINE+data.summary.byHealth.NEEDS_ATTENTION+data.summary.byHealth.AUTH_DEGRADED}</strong><span>דורשים טיפול</span></article><article className="do-metric"><HardDrive/><strong>{data.summary.backlogRecords}</strong><span>פריטים ממתינים לסנכרון</span></article></section>
  <section className="do-panel"><div className="do-section-head"><div><h2>צי רכיבי החיבור</h2><p>רכיב חיבור אחד עשוי לשרת כמה מצלמות. ערוצים ריקים אינם נספרים כמצלמות כושלות.</p></div><Activity/></div><div className="do-summary-list">{data.devices.map(device=><div key={device.id}><span>{device.siteName} · {labels[device.profile]||device.profile} · {device.runtimeVersion||"גרסה לא ידועה"}</span><strong className={`do-badge ${device.health==="HEALTHY"?"good":device.health==="RECOVERING"?"warn":"bad"}`}>{labels[device.health]} · {device.dependentPhysicalCameras} מצלמות</strong></div>)}</div></section>
  <section className="do-grid cols-2"><article className="do-panel"><h2>גרסאות ותצורה</h2><div className="do-summary-list">{data.summary.versions.map(([version,count])=><div key={version}><span>{version}</span><strong>{count}</strong></div>)}</div></article><article className="do-panel"><h2>התראות תשתית ממוזגות</h2>{data.alerts.length?<div className="do-summary-list">{data.alerts.map(alert=><div key={alert.dedupeKey}><span>{alert.message}</span><strong>{alert.affectedPhysicalCameras}</strong></div>)}</div>:<div className="do-empty compact"><ShieldCheck/><strong>אין התראות תשתית פעילות</strong></div>}</article></section>
  <section className="do-panel"><div className="do-section-head"><div><h2>פעולות בטוחות</h2><p>בדיקת בריאות, רענון תצורה, גילוי מחדש ועדכון מאושר נשלחים עם תוקף קצר, הרשאה, תצוגה מקדימה ותיעוד.</p></div><RefreshCw/></div><div className="do-notice info"><span>פעולה קבוצתית דורשת בחירת Tenant/Site מפורשת ואישור. אין shell מרוחק ואין פעולה גלובלית עמומה.</span></div></section></div></ObserverAppShell>}
