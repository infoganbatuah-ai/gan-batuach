import { DashboardLoadingState } from "@/components/dashboard-loading-state";

export default function Loading() {
  return <DashboardLoadingState title="טוען מרכז אדמין" body="בודקים גנים, משתמשים, פיקוחים, מצלמות, AI ומשימות בלי להציג מסך ריק." />;
}
