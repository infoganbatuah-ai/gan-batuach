import { AppRegisterEntryScreen } from "@/components/app-register-screens";

export const metadata = {
  title: "הרשמה | גן בטוח",
  description: "בחירת מסלול הרשמה אפליקטיבי: הורה, מנהלת גן, צוות או מפקח."
};

export default function AppRegisterPage() {
  return <AppRegisterEntryScreen />;
}
