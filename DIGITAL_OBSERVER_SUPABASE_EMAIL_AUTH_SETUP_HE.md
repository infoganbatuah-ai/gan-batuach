# הגדרת אימות מייל לתצפיתן הדיגיטלי

הקוד במערכת תומך כעת בשתי דרכי אימות של Supabase: קוד חד-פעמי שמוזן במסך האימות, וקישור אימות שמגיע ישירות לנתיב שרת מאובטח.

## מצב מאומת ב-20.8.2026

- הרשמה בדוא״ל פעילה ודרישת אישור מייל פעילה ב-Supabase.
- בדיקת Admin לקריאה בלבד מצאה 41 משתמשי Auth, אך לא מצאה משתמש תצפיתן חדש או משתמש חדש שממתין לאימות.
- לכן בקשת הבדיקה האחרונה לא יצרה הרשמת תצפיתן חדשה שממנה יכול להישלח קוד.
- במשתני הסביבה המקומיים אין ספק מייל או מפתח Resend/SendGrid. הגדרת SMTP של Supabase נשמרת בפרויקט עצמו ויש לבדוק אותה ב-Dashboard.
- בלי Custom SMTP, שירות ברירת המחדל של Supabase שולח רק לכתובות השייכות לחברי צוות הפרויקט ומוגבל לשתי הודעות בשעה.
- שימוש חוזר בכתובת שכבר קיימת ומאומתת אינו יוצר הרשמה חדשה ואינו שולח קוד הרשמה נוסף.
- בדיקת Dashboard אישרה ש-`Enable custom SMTP` כבוי ושהפרויקט משתמש בשירות המייל המובנה והמוגבל של Supabase.
- תבנית `Confirm sign up` עודכנה ונשמרה ב-Supabase עם `{{ .Token }}` וקישור שרת המבוסס על `{{ .TokenHash }}`.
- כתובת הבדיקה שסופקה כבר קיימת ומאומתת ומשויכת בגן בטוח; לכן היא מיועדת למסלול התחברות לחשבון קיים ולא להרשמה חוזרת.
- בדיקת `Auth settings` ישירה עברה: הרשמה פעילה, אישור מייל נדרש וספק email מופעל ברמת Auth.
- בדיקת הבריאות הישנה השתמשה בנתיב REST שדורש מפתח סודי עם מפתחות Supabase החדשים ולכן הציגה `supabase: unreachable` בטעות. הקוד תוקן לבדוק את נתיב Auth שבו הרישום משתמש בפועל.
- ב-Dashboard אין כרגע Secret API Key פעיל. הערך המקומי של `SUPABASE_SERVICE_ROLE_KEY` אינו רשום עוד ולכן פעולות שרת מורשות ייכשלו עד שדניאל ייצור/יעתיק מפתח שרת תקין באופן ידני ומאובטח.
- בעת האבחון החסם לשליחת קוד לכתובת חדשה היה `Custom SMTP` כבוי. החסם הוסר ב-21.8.2026 ונבדק בהרשמה סינתטית כמפורט להלן.

## עדכון קבלה ב-21.8.2026

- דניאל הפעיל Custom SMTP דרך חשבון Gmail הייעודי.
- Supabase טען מחדש את הגדרת Auth והעלה את מכסת שליחת המיילים מ-2 ל-30 לשעה.
- בוצעה הרשמת בדיקה סינתטית ל-`info.ganbatuah+observer-auth-test-20260820210400@gmail.com`.
- בקשת `/signup` הסתיימה בהצלחה ללא שגיאת SMTP.
- משתמש Auth סינתטי נוצר עם `Confirmation sent at` מלא ו-`Confirmed at` ריק, כלומר Supabase מסר את הודעת האימות לספק והממתין טרם אישר אותה.
- אימות הגעה ל-Inbox/Spam עדיין דורש כניסה ידנית ל-Gmail. אין למחוק את משתמש הבדיקה לפני בדיקת המייל, כדי שהקישור יישאר תקף.

## תיקון סביבת Vercel ואימות חי ב-21.8.2026

- סביבת ה-Production וה-Preview של Vercel עודכנה עם כתובת פרויקט Supabase והמפתח הציבורי הפעיל של הפרויקט `kuaywzvucllxjsxarogb`. ערכי המפתחות עצמם לא הודפסו ולא נשמרו במסמך.
- בוצעה פריסת Production חדשה והכתובת `https://gan-batuach.vercel.app` עודכנה לפריסה התקינה.
- בדיקת `https://gan-batuach.vercel.app/api/health` החזירה HTTP 200 עם `supabase: ok`.
- בוצעה הרשמת תצפיתן סינתטית דרך אתר ה-Production. הטופס עבר בהצלחה ל-`/digital-observer/verify` ולא הציג עוד שגיאת מפתח או סביבת Supabase.
- ב-Supabase Auth נוצר משתמש הבדיקה עם `Confirmation sent at` מלא ו-`Confirmed at` ריק. משמעות הדבר היא ש-Supabase התחיל את שליחת הודעת האימות דרך ה-SMTP, אך הקוד עדיין לא הוזן ולא אושר.
- לא נמשכו מ-Vercel מפתחות שרת או סודות. `SUPABASE_SERVICE_ROLE_KEY` לא נחשף ולא שונה במסגרת תיקון זה.
- הבדיקה הידנית היחידה שנותרה למסלול המייל היא לבדוק ב-Gmail את Inbox, Spam ו-All Mail, לפתוח את ההודעה ולהזין את הקוד במסך האימות.

## תיקון קישור האימות והשלמת החשבון ב-21.8.2026

- יומן Supabase הוכיח שהאימות הראשון של המשתמש הצליח. ניסיון שני עם אותו קוד החזיר `otp_expired`, מפני שקוד חד-פעמי נצרך כבר באימות הראשון.
- מקור הקריסה לאחר הלחיצה במייל נמצא בהגדרת Auth: כתובת האתר הייתה `http://localhost:3000` ולא היו כתובות Redirect מאושרות.
- הגדרת Supabase עודכנה בפועל: `Site URL` הוא `https://gan-batuach.vercel.app`, ונוספו נתיבי `auth/confirm` ו-`auth/callback` עבור Production ועבור localhost לפיתוח בלבד.
- המיגרציה `20260821000100_digital_observer_verified_account_sync.sql` הוחלה ב-Supabase בהצלחה.
- בדיקת SQL לקריאה בלבד אישרה שפונקציית הסנכרון קיימת, טריגר אישור המייל קיים, ולכל משתמש תצפיתן מאומת שנמצא בבדיקה קיים גם חשבון תצפיתן.
- לאחר אימות מוצלח, כשל זמני בהכנת חשבון המוצר אינו מחזיר עוד את המשתמש למסך קוד עם קוד שכבר נוצל. הכשל נרשם בצד השרת, והמשתמש מופנה להתחברות; הטריגר במסד משלים את חשבון התצפיתן.
- קוד האימות והכפתור במייל הם שתי חלופות לאותו אימות. אין להשתמש בשניהם בזה אחר זה.
- המשתמש שאומת בבדיקה כבר מאושר. הפעולה הנכונה עבורו היא להתחבר ב-`/digital-observer/login`, בלי להירשם שוב ובלי להזין שוב את הקוד הישן.

## שחזור הרשמה לכתובת קיימת ב-21.8.2026

- רישום חוזר של כתובת שכבר קיימת אינו תלוי עוד בכך ש-Supabase ישלח שוב את הודעת `Confirm sign up`. המערכת מזהה את התוצאה המוסווית של משתמש קיים ושולחת קוד כניסה ואימות חדש דרך מסלול `Magic link or OTP` עם `shouldCreateUser: false`.
- הכפתור `שליחת קוד חדש והמשך הרשמה` משתמש באותו מסלול ולכן מתאים גם למשתמש מאומת וגם למשתמש שהרשמתו הקודמת נתקעה.
- תבנית `Magic link or OTP` ב-Supabase עודכנה בעברית וכוללת גם `{{ .Token }}` וגם כפתור ישיר ל-`/auth/confirm` עם הקשר מפורש של התצפיתן הדיגיטלי.
- מגבלת המיילים בפרויקט הועלתה באופן מבוקר מ-30 ל-60 הודעות בשעה. מגבלות OTP לפי IP נשארו פעילות כדי לא להסיר הגנת שימוש לרעה.
- המיגרציה `20260821000200_digital_observer_existing_identity_recovery.sql` הוחלה. היא מאפשרת למשתמש מאומת ליצור או לתקן רק את פרופיל וחשבון התצפיתן של עצמו, בלי לשנות תפקיד קיים בגן בטוח ובלי גישה לחשבון אחר.
- מסלול ההתחברות מכין את חשבון התצפיתן לפני טעינת הפרופיל והיעד, ולכן משתמש Auth קיים אינו אמור עוד ליפול בגלל פרופיל מוצר חסר.

לפני ניסיון נוסף יש לבדוק **Authentication -> Logs** ו-**Authentication -> SMTP Settings**. אם הכתובת כבר קיימת, יש להתחבר איתה או למחוק רק את משתמש הבדיקה המדויק לאחר אימות שאינו משתמש אמיתי.

## 1. כתובות Supabase

ב-Supabase Dashboard פתחו **Authentication -> URL Configuration** והגדירו:

- Site URL: `https://gan-batuach.vercel.app`
- Redirect URL: `https://gan-batuach.vercel.app/auth/confirm`
- Redirect URL: `https://gan-batuach.vercel.app/auth/callback`
- לפיתוח מקומי בלבד: `http://localhost:3000/auth/confirm`
- לפיתוח מקומי בלבד: `http://localhost:3000/auth/callback`

## 2. תבנית Confirm signup

פתחו **Authentication -> Email Templates -> Confirm signup**. התבנית צריכה להציג גם קוד וגם קישור:

```html
<h2>אימות חשבון</h2>
<p>קוד האימות שלך:</p>
<p style="font-size:28px;font-weight:700;letter-spacing:6px">{{ .Token }}</p>
<p>אפשר גם לאמת בלחיצה:</p>
<p><a href="{{ .SiteURL }}/auth/confirm?token_hash={{ .TokenHash }}&type=email">אימות החשבון</a></p>
```

תבנית `Confirm signup` משותפת כרגע לשני המוצרים, ולכן הכותרת נשארת כללית. נתיב האימות מזהה את המוצר מתוך החשבון שאומת: חשבון תצפיתן חוזר להתחברות של התצפיתן, וחשבון גן בטוח אינו נשלח אליו. יש להשבית link tracking אצל ספק המייל כדי שספק חיצוני לא ישכתב את קישור האימות.

## 3. ספק מייל

הדרך הפשוטה היא Resend:

1. צרו חשבון Resend והוסיפו דומיין שבבעלותכם.
2. הוסיפו ב-DNS את הרשומות ש-Resend מציג והמתינו לאימות הדומיין.
3. ב-Resend פתחו Integrations, בחרו Supabase וחברו את הפרויקט `gan-batuah`.
4. ב-Supabase ודאו תחת Authentication -> Email/SMTP שהספק הפעיל הוא Resend ושהשולח משתמש בדומיין המאומת.
5. תחת Authentication -> Rate Limits הגדירו מכסה מתאימה לפיילוט מצומצם.

אין להכניס API key למסמך, לקוד או למשתנה שמתחיל ב-`NEXT_PUBLIC_`.

### מה חסר כרגע בפועל

1. לפרוס את תיקון הקוד ל-Production.
2. להתחבר עם המשתמש שכבר אומת ולוודא שהוא ממשיך למסלול `/digital-observer/onboarding` ולא למסלול גננת של גן בטוח.
3. בבדיקת הרשמה חדשה, לבחור דרך אימות אחת בלבד: לחיצה על הכפתור במייל או הזנת הקוד.
4. לאחר שהבדיקה הושלמה, למחוק רק את משתמשי הבדיקה הסינתטיים שאינם נחוצים עוד.

חיבור ה-SMTP, תיקון משתני Vercel וה-redeploy כבר הושלמו. אין צורך לבצע אותם שוב לפני בדיקת ה-Inbox.

### מסלול זמני אם עדיין אין דומיין לשליחה

אפשר לחבר את חשבון `info.ganbatuah@gmail.com` כ-SMTP זמני באמצעות Google App Password, רק לצורך בדיקות מצומצמות. צריך להפעיל אימות דו-שלבי בחשבון Google וליצור App Password ייעודי. את הסיסמה הייעודית מזינים ישירות ב-Supabase ולא בקוד או בצ'אט. זהו פתרון בדיקה זמני; למסירה מסחרית עדיף Resend עם דומיין מאומת.

ב-Supabase, תחת **Authentication -> Emails -> SMTP Settings**, יש להזין:

- Sender email: `info.ganbatuah@gmail.com`
- Sender name: `גן בטוח | תצפיתן דיגיטלי`
- Host: `smtp.gmail.com`
- Port: `465`
- Username: `info.ganbatuah@gmail.com`
- Password: ה-Google App Password בן 16 התווים, בהדבקה ישירה בלבד

אחרי השמירה יש לבצע ניסיון עם כתובת חדשה ולבדוק גם Inbox וגם Spam. אין להשתמש בסיסמה הרגילה של חשבון Google.

## 4. בדיקת קבלה

1. מחקו רק את משתמש הבדיקה המדויק שבו רוצים להשתמש מחדש.
2. פתחו `/digital-observer/register` בחלון פרטי.
3. הרשמו עם כתובת בדיקה חדשה.
4. ודאו שמגיע מייל ושמופיע בו קוד בן 6-8 ספרות וקישור.
5. הזינו את הקוד ב-`/digital-observer/verify`.
6. ודאו שהמערכת עוברת ל-`/digital-observer/login?verified=1`.
7. התחברו ובדקו שהיעד הוא `/digital-observer/onboarding`, ולא מסלול גננת.
8. בחרו בית/עסק וחבילה. המערכת יוצרת ניסיון של 14 יום ללא חיוב אמיתי עד חיבור ספק תשלום.

## 5. מחיקת משתמש בדיקה קיים

יש למחוק ב-Supabase רק לפי כתובת מייל מדויקת: **Authentication -> Users -> חיפוש המייל -> Delete user**. מחיקת Auth user מוחקת גם את פרופיל הבדיקה והנתונים המקושרים בהתאם ליחסי המחיקה במסד. לפני מחיקה יש לוודא שאין זה משתמש אמיתי או משתמש של גן בטוח.

קיים גם כלי מקומי שמסרב לפעול ב-production ומוחק רק חשבון שמזוהה כתצפיתן. הוא דורש כתובת מדויקת ואישור מפורש; אין להריץ אותו על כתובת שלא נבדקה ידנית.

לפני בדיקת הרישום יש להחיל ב-Supabase SQL Editor את המיגרציה:

`supabase/migrations/20260820020000_digital_observer_auth_trial_separation.sql`
