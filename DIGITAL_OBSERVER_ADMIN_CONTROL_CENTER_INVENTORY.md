# מיפוי מרכז האדמין של התצפיתן הדיגיטלי

תאריך: 2026-08-21
סטטוס: הוטמע ונבדק מקומית, טרם נדחף וטרם נפרס

## קו הקבלה

ההשוואה בוצעה מול תמונת `תשלומים, מנויים וניהול מערכת.png`, ובפרט מסך "ניהול מערכת", וכן מול שפת המוצר העסקית והמובייל בצילומי התצפיתן. התמונות הן קו קבלה מחייב ולא השראה כללית.

| דרישה | מצב לפני העבודה | מצב נוכחי | הוכחה |
|---|---|---|---|
| מרכז בקרה עצמאי לתצפיתן | מסכי ניהול חלקיים המבוססים על `admin` הכללי של גן בטוח | מרכז עצמאי ב-`/digital-observer/admin` עם מעטפת וניווט ייעודיים | `app/digital-observer/admin/page.tsx` |
| מצב מנוע ומערכת | דף דוחות ללא היררכיית control center מלאה | מצב מנוע Shadow, התראות תצורה ובריאות שירותים ללא ירוק מזויף | מסך מרכז הבקרה |
| לקוחות ביתיים ועסקיים | לא הוצג כמדד רוחבי אחד | מדדים מופרדים ביתי/עסקי ומסך לקוחות ואתרים | `/digital-observer/admin/access` |
| מפת מיקומים מורשים | חסר | פריסת מיקומים לפי קואורדינטות מאומתות ורשימת כתובות; לא מומצאות נקודות | מקטע "מפת לקוחות ואתרים מורשים" |
| אירועים ומגמות | חלקי | מגמת 7 ימים לפי זמן ישראל וסוגי אירוע שכיחים מנתוני התצפיתן | מקטע "אירועים ומגמה" |
| מקורות נתונים: אתרים, מצלמות, אירועים ומנויים | מפוזר בין מסכים | טבלה מרוכזת והפרדה מפורשת מנתוני גן בטוח | `lib/domain/digital-observer/admin-runtime.ts` |
| תורים, מדיניות ולמידה | חלקי | תור בדיקה אנושי, מסירות, כללי ניטור ואתרים בתהליך למידה | `/digital-observer/admin/operations` |
| מנויים, חבילות וחיוב ניהולי | קיים חלקית תחת הרשאת admin כללית | מסכים ייעודיים עם Mock/Readiness וחסימה של הפעלה מסחרית חיה | `/digital-observer/admin/billing`, `/packages` |
| הרשאת אדמין נפרדת | חסר | claim חתום `digital_observer_admin` ללא הרשאת admin כללית של גן בטוח | `lib/domain/digital-observer/admin-access.ts` |
| מובייל-first | לא היה קו קבלה ייעודי לאדמין | ניווט תחתון בן 5 יעדים, כותרות מלאות וגרידים מותאמים | `components/digital-observer/observer-app-shell.tsx`, CSS |
| loading/error אמיתיים | חסר במסלול האדמין | נוספו מצבי טעינה ושגיאה ללא fallback מזויף | `app/digital-observer/admin/loading.tsx`, `error.tsx` |
| משתמש פיילוט נפרד | לא קיים | תהליך יצירה בטוח ותהליך set-password חד-פעמי מוכנים; היצירה עצמה חסומה במפתח ניהול מקומי לא תקף | `scripts/qa/create-digital-observer-admin-pilot.mjs` |

## גבולות אמת ובטיחות

- מרכז האדמין קורא מטא-דאטה שנבחר במפורש. הוא אינו בוחר RTSP, סיסמאות מצלמה, tokens, פרטי חיבור סודיים או וידאו.
- אתרי גן בטוח (`kindergarten` או אתר עם `garden_id`) אינם נכנסים למרכז האדמין העצמאי.
- מיקום ללא קואורדינטות מאומתות מוצג ברשימה בלבד ולא מקבל נקודה מומצאת במפה.
- Gateway/DVR, AI חי, ביומטריה, הודעות חיות, חיוג חירום וחיוב חי נשארו כבויים.
- פעולות שאינן מחוברות מסומנות `Shadow`, `הדמיה בטוחה`, `readiness` או `Mock`.

## מה עדיין תלוי בתשתית

1. מפתח `SUPABASE_SERVICE_ROLE_KEY` המקומי אינו תקף ולכן מקור הנתונים הניהולי אינו זמין בהרצה המקומית. ה-UI מציג זאת ביושר ולא ממציא נתונים.
2. חשבון הפיילוט הייעודי לא נוצר. הסקריפט נעצר לפני יצירה ולא הדפיס סיסמה.
3. מפה גאוגרפית מלאה עם שכבת בסיס דורשת ספק מפות מורשה; כרגע מוצגת פריסת קואורדינטות מאומתות בלבד.
4. אין הוכחה לשירותים חיים, בהתאם לאיסור ההפעלה בשלב זה.
