# Management document and file inventory — GB-M32

Date: 2026-09-20. Scope: Development integration baseline `3105b440383ea235abde76aa62f2727823fa31ee`; this is source and isolated-Development evidence, not Production bucket proof.

| Domain | Canonical data | Bucket / route | Access and current risk | GB-M32 treatment |
| --- | --- | --- | --- | --- |
| Garden, Child, Staff, Owner/Teacher, Guardian | `public.documents`, originally created in `20260523000000_initial_schema.sql` | `documents`; `/api/documents`, `/api/documents/[id]/review` | Bucket private in isolated Development, but legacy `file_url` can be a time-limited signed URL stored as though permanent. Historical read/write RLS grants were broad, including uploader update. Expiry existed as a date but was not consistently evaluated. | Reuse table; add explicit owner/storage/version/retention fields, row-scope read, server-only writes and private signed read route. Legacy URLs remain for manual provenance review and are hidden from updated UI. |
| Garden onboarding | Existing Garden onboarding document fields and Garden upload panel | Previously generic `/api/storage/upload` to `documents` | Generic service-role upload accepted client bucket/prefix and returned signed URL for later persistence. | Garden upload panel now calls scoped document upload; existing onboarding records are not guessed/backfilled. GB-M10 handoff requires a separate live journey check. |
| Staff onboarding, qualifications | `public.staff`, `public.documents`; Staff document panels | Previously generic `/api/storage/upload` | UI used legacy `profile.garden_id` and mixed certificate states. | Staff document and background pages now use active employment context; upload is pending review, never self-verified. |
| Child medication approval | `public.child_health_records.medication_approval_url`, `public.documents` | Previously generic `documents` upload | Arbitrary URL could be submitted as medical approval reference. | New uploads create Child-scoped `medical_approval` document; update route verifies exact private document, Child and Garden before linking. Existing external URL rows are preserved, not represented as canonical private files. |
| Inspection documents/evidence | `public.inspections`, inspection evidence references | `inspection-reports`; `/api/inspections/[id]/evidence` | Private bucket in isolated Development; dedicated route signs for 60 seconds. Evidence semantics are immutable inspection facts. | Kept separate. Generic elevated upload can no longer target bucket. New persistent `inspection_document` category is supported but does not replace evidence. |
| Corrective-action evidence | Violation/action evidence references | `inspection-reports`; `/api/violations/[id]/evidence` | Private domain route; evidentiary workflow. | Kept separate from business documents. |
| Complaints | Complaint attachment references | `/api/parent/complaints`; private attachment contract | Public/raw attachment URLs rejected by Parent complaint route. | Kept separate; no conversion into `documents`. |
| Messaging | `public.management_message_attachments` | `management-message-attachments`; dedicated GB-M29 routes | Private bucket; participant-checked 60-second signed retrieval. | Kept separate; message attachment does not become a business document. |
| Digital Observer | Separate Safety/evidence tables and storage | Digital Observer routes/buckets | Independent provenance, retention and authorizations. | Outside Management migration; core diff zero. |

The isolated Development bucket metadata reports `documents`, `inspection-reports`, and `management-message-attachments` as private. This does not prove Production configuration or a live anonymous object test. The new Management path is `management/{garden UUID}/{document UUID}/{object UUID}.{pdf|jpg|png|webp}`; only the server chooses it. No bucket is newly purchased or created.

Outstanding legacy review: classify `documents.file_url` rows by actual object provenance before deterministic migration; review old generic uploads, historical signed links in other domain records, category-specific retention, Storage restore evidence, malware scanning, and orphan objects. No automatic deletion or migration of unknown customer files is authorized by this inventory.
